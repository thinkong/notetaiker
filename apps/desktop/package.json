{
  "name": "@notetaiker/desktop",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "dev": "bunx electrobun dev",
    "build": "bunx electrobun build",
    "build:stable": "bunx electrobun build --env=stable",
    "build:all": "bunx electrobun build --targets macos-arm64,win-x64,linux-x64"
  },
  "dependencies": {
    "@notetaiker/api": "workspace:*",
    "electrobun": "latest"
  },
  "devDependencies": {
    "@types/node": "^25.3.3"
  }
}
