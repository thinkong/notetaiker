---
phase: 21-graph-interactions
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - /home/ubuntu/projects/notetaiker/apps/web/src/components/graph/ForceGraph.tsx
  - /home/ubuntu/projects/notetaiker/apps/web/src/components/graph/GraphTooltip.tsx
  - /home/ubuntu/projects/notetaiker/apps/web/src/components/graph/GraphView.tsx
autonomous: true
must_haves:
  truths:
    - "User can double-click a node to open the note in the editor"
    - "User can hover a node to see a frosted-glass tooltip with excerpt"
    - "User can single-click a node to highlight connections"
  artifacts:
    - path: "/home/ubuntu/projects/notetaiker/apps/web/src/components/graph/GraphTooltip.tsx"
      provides: "HTML overlay for graph tooltips"
  key_links:
    - from: "/home/ubuntu/projects/notetaiker/apps/web/src/components/graph/ForceGraph.tsx"
      to: "/home/ubuntu/projects/notetaiker/apps/web/src/components/graph/GraphTooltip.tsx"
      via: "Component render"
---

<objective>
Implement advanced interactions: double-click navigation, rich tooltips, and refined selection visuals.
Purpose: Transform the graph from a passive view to an active navigation tool.
Output: An interactive ForceGraph with tooltips and navigation logic.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-graph-interactions/21-CONTEXT.md
@.planning/phases/21-graph-interactions/21-RESEARCH.md
@apps/web/src/components/graph/ForceGraph.tsx
@apps/web/src/components/graph/GraphView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Interaction Logic (Click vs Double-Click)</name>
  <files>/home/ubuntu/projects/notetaiker/apps/web/src/components/graph/ForceGraph.tsx</files>
  <action>
    Implement the click discriminator pattern from Research.
    - Refs: `clickTimeoutRef`.
    - Logic: On click, wait 300ms. If second click comes, cancel timer and trigger `onNodeDoubleClick`. If not, trigger `onNodeClick`.
    - Props: Add `onNodeDoubleClick` to ForceGraph props.
    - Behavior:
      - Single click: Highlight node and connections (existing logic, ensure it persists).
      - Double click: Call prop (will navigate).
  </action>
  <verify>
    Verify single click still highlights. Verify double click triggers distinct action.
  </verify>
  <done>ForceGraph differentiates single and double clicks.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Tooltip Component & Overlay</name>
  <files>
    /home/ubuntu/projects/notetaiker/apps/web/src/components/graph/GraphTooltip.tsx
    /home/ubuntu/projects/notetaiker/apps/web/src/components/graph/ForceGraph.tsx
  </files>
  <action>
    1. Create `GraphTooltip.tsx`:
       - Props: `node` (GraphNode), `x`, `y`.
       - Style: Tailwind, frosted glass (`backdrop-blur-md bg-white/80 dark:bg-nord-polar1/80`), shadow, rounded.
       - Content: Title (`node.name`) + Excerpt (`node.content` sliced to 2 lines).
    2. Integrate in `ForceGraph.tsx`:
       - State: `hoveredNode`, `tooltipPos` {x, y}.
       - Update `handleNodeHover`: Set hovered node.
       - Use `onRenderFramePost` or `onZoom` to update tooltip coordinates using `graph2ScreenCoords`.
       - Render `GraphTooltip` as a sibling to `ForceGraph2D` (inside a relative container).
  </action>
  <verify>
    Hover node -> Tooltip appears. Pan graph -> Tooltip follows node.
  </verify>
  <done>Rich tooltips render over graph nodes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Navigation & Visual Polish</name>
  <files>
    /home/ubuntu/projects/notetaiker/apps/web/src/components/graph/GraphView.tsx
    /home/ubuntu/projects/notetaiker/apps/web/src/components/graph/ForceGraph.tsx
  </files>
  <action>
    1. In `GraphView.tsx`:
       - Pass `onNodeDoubleClick` handler.
       - Handler logic: `navigate("/", { state: { noteId: node.id } })`.
    2. In `ForceGraph.tsx`:
       - Refine `paintNode`:
         - Ensure `backdrop-blur` isn't simulated in Canvas (we use the HTML tooltip now).
         - Highlight colors: Node glow, thick edges.
         - Dimming: Reduce opacity of non-neighbors to 50% (already exists, verify parameters).
  </action>
  <verify>
    Double clicking a node navigates to home and loads note.
  </verify>
  <done>Navigation connected and visuals polished.</done>
</task>

</tasks>

<verification>
1. Hover node: Tooltip appears with excerpt.
2. Single click: Node highlights, others dim.
3. Double click: Navigates to editor with note open.
</verification>

<success_criteria>
- Tooltips are HTML (accessible, styled) not Canvas text.
- Navigation works via double-click.
- Interaction feels responsive.
</success_criteria>

<output>
After completion, create `.planning/phases/21-graph-interactions/21-02-SUMMARY.md`
</output>
