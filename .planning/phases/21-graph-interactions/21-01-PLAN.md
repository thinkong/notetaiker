---
phase: 21-graph-interactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/ubuntu/projects/notetaiker/apps/web/src/contexts/GraphStateContext.tsx
  - /home/ubuntu/projects/notetaiker/apps/web/src/App.tsx
  - /home/ubuntu/projects/notetaiker/apps/web/src/components/graph/GraphView.tsx
  - /home/ubuntu/projects/notetaiker/apps/web/src/components/graph/ForceGraph.tsx
autonomous: true
must_haves:
  truths:
    - "User can leave the graph view and return to find it exactly as they left it (zoom/pan)"
    - "User can navigate to the capture view with a specific note pre-loaded via URL/State"
    - "Graph zoom and center point persist across navigation"
  artifacts:
    - path: "/home/ubuntu/projects/notetaiker/apps/web/src/contexts/GraphStateContext.tsx"
      provides: "Persistence for graph camera state"
      exports: ["GraphStateProvider", "useGraphState"]
  key_links:
    - from: "/home/ubuntu/projects/notetaiker/apps/web/src/components/graph/GraphView.tsx"
      to: "/home/ubuntu/projects/notetaiker/apps/web/src/contexts/GraphStateContext.tsx"
      via: "useGraphState hook"
---

<objective>
Implement graph state persistence and enable deep linking to notes in the editor.
Purpose: Ensure the graph feels stable (doesn't reset on every visit) and establish the wiring needed for "Double-click to navigate".
Output: A GraphStateContext that persists view coordinates, and an updated App that handles incoming note navigation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-graph-interactions/21-CONTEXT.md
@.planning/phases/21-graph-interactions/21-RESEARCH.md
@apps/web/src/components/graph/GraphView.tsx
@apps/web/src/components/graph/ForceGraph.tsx
@apps/web/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GraphStateContext</name>
  <files>/home/ubuntu/projects/notetaiker/apps/web/src/contexts/GraphStateContext.tsx</files>
  <action>
    Create a context to store graph view state.
    - State: `zoom` (number), `center` ({x, y}), `selectedNodeId` (string | null).
    - Provide `setGraphState` and `graphState`.
    - Wrap the export in a Provider component.
    Use this to persist state even when GraphView unmounts (e.g. navigating to Editor).
  </action>
  <verify>
    Check file creation and exports.
  </verify>
  <done>Context created with Provider and Hook.</done>
</task>

<task type="auto">
  <name>Task 2: Enable Note Navigation in App.tsx</name>
  <files>/home/ubuntu/projects/notetaiker/apps/web/src/App.tsx</files>
  <action>
    Update `MainCapture` component to handle incoming navigation state.
    - Import `useLocation` from react-router-dom.
    - Add `useEffect` to check `location.state?.noteId`.
    - If present, call `handleEditNote(location.state.noteId)` to load the note.
    - Ensure this only runs once per navigation (clear state or track dependency).
    - Wrap `GraphView` route in `GraphStateProvider` (or wrap `App` if state should persist longer). Note: Wrap `RouterProvider` or `Layout` to ensure context exists. Prefer wrapping `GraphView` specifically if we only care about within-session, but wrapping `Layout` is safer for preserving state across tab switching if we add local storage later. For now, wrap `GraphView` in the route definition or inside the component. Actually, wrap `RouterProvider` in `App` component to be safe.
  </action>
  <verify>
    Check App.tsx for useLocation and handleEditNote wiring.
  </verify>
  <done>MainCapture loads note from location state.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Persistence in GraphView</name>
  <files>
    /home/ubuntu/projects/notetaiker/apps/web/src/components/graph/GraphView.tsx
    /home/ubuntu/projects/notetaiker/apps/web/src/components/graph/ForceGraph.tsx
  </files>
  <action>
    1. In `GraphView.tsx`:
       - Use `useGraphState`.
       - Pass `initialZoom` and `initialCenter` props to `ForceGraph`.
       - On unmount (or before navigate), save current state from `ForceGraph` ref to Context.
    2. In `ForceGraph.tsx`:
       - Add props `initialZoom`, `initialCenter`.
       - In `useEffect`, after graph init, apply these values using `fgRef.current.zoom()` and `centerAt()`.
       - Expose a method or allow parent to access ref to get current zoom/center for saving.
  </action>
  <verify>
    Navigate to graph, pan/zoom, navigate away, return. View should be restored.
  </verify>
  <done>Graph view restores position on reload/return.</done>
</task>

</tasks>

<verification>
1. User navigates to /graph.
2. User pans and zooms.
3. User navigates back to Home.
4. User navigates to /graph -> View is restored.
5. User navigates to Home with state `{noteId: "..."}` -> Note loads.
</verification>

<success_criteria>
- GraphStateContext exists and provides state.
- App.tsx accepts navigation to specific note.
- GraphView persists zoom/pan levels.
</success_criteria>

<output>
After completion, create `.planning/phases/21-graph-interactions/21-01-SUMMARY.md`
</output>
