---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [apps/web/package.json, apps/web/src/App.tsx, apps/web/src/lib/api.ts, apps/web/src/index.css, apps/web/vite.config.ts]
autonomous: true

must_haves:
  truths:
    - "Web app renders 'Hello NoteTaiker'"
    - "Tailwind v4 styles are applied and visible"
    - "Hono client is initialized with AppType from @notetaiker/api"
  artifacts:
    - path: "apps/web/src/lib/api.ts"
      provides: "Type-safe RPC client"
    - path: "apps/web/src/index.css"
      provides: "Tailwind v4 entry point"
  key_links:
    - from: "apps/web/src/lib/api.ts"
      to: "apps/api/src/index.ts"
      via: "import type { AppType }"
---

<objective>
Establish the frontend foundation using React, Vite, and Tailwind CSS v4, integrated with the API via Hono RPC.

Purpose: Provide a fast, type-safe development environment for the capture interface.
Output: Functional React SPA in apps/web with Tailwind v4 and RPC client.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold apps/web with Vite and Tailwind v4</name>
  <files>apps/web/package.json, apps/web/vite.config.ts, apps/web/src/index.css, apps/web/src/main.tsx, apps/web/tsconfig.json, apps/web/eslint.config.js</files>
  <action>
    Initialize apps/web with Vite (React + TS template).
    Install tailwindcss @tailwindcss/vite.
    Configure vite.config.ts with the tailwind plugin.
    Set up src/index.css with @import "tailwindcss"; and @source declarations for monorepo support.
    Modify tsconfig.json to extend @notetaiker/tsconfig/base.json.
    Create eslint.config.js extending @notetaiker/eslint-config/base.js.
  </action>
  <verify>pnpm -F @notetaiker/web build && grep -q "tailwindcss" apps/web/dist/assets/*.css</verify>
  <done>Vite/React/Tailwind v4 stack is operational and follows workspace standards.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Hono RPC client</name>
  <files>apps/web/src/lib/api.ts, apps/web/src/App.tsx</files>
  <action>
    Add @notetaiker/api as a workspace dependency to apps/web.
    Create src/lib/api.ts using Hono's 'hc' (honeyclient) to create a type-safe client using AppType.
    Update App.tsx to perform a test fetch from /health and display the result.
  </action>
  <verify>pnpm -F @notetaiker/web build && grep -q "status" apps/web/dist/assets/*.js</verify>
  <done>Frontend can communicate with backend with full type safety.</done>
</task>

</tasks>

<verification>
Confirm the web app runs, styles work, and API data is displayed.
</verification>

<success_criteria>
Web foundation is complete with type-safe backend communication.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
