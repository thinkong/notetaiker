---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [apps/api/package.json, apps/api/src/index.ts, apps/api/tsconfig.json]
autonomous: true

must_haves:
  truths:
    - "API server starts and responds to /health"
    - "API exports AppType for frontend RPC consumption"
  artifacts:
    - path: "apps/api/src/index.ts"
      provides: "Hono entry point"
    - path: "apps/api/package.json"
      provides: "API dependencies (hono)"
  key_links:
    - from: "apps/api/src/index.ts"
      to: "AppType export"
      via: "export type"
---

<objective>
Establish the backend foundation using Hono, configured for type-safe RPC.

Purpose: Provide a lightweight, high-performance API that shares types with the frontend.
Output: Functional Hono API scaffold in apps/api.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold apps/api with Hono</name>
  <files>apps/api/package.json, apps/api/src/index.ts, apps/api/tsconfig.json</files>
  <action>
    Create apps/api directory. Initialize package.json with name '@notetaiker/api', hono, and @hono/node-server.
    Add 'build', 'dev', and 'lint' scripts to apps/api/package.json.
    Configure tsconfig.json to extend @notetaiker/tsconfig/base.json.
    Create src/index.ts with a basic Hono app, a /health route returning {status: "ok"}, and export the 'app' as AppType.
    Ensure the server listens on port 3001 and initializes '@notetaiker/env' validation at startup.
  </action>
  <verify>pnpm -F @notetaiker/api run lint && (pnpm -F @notetaiker/api run dev & PID=$!; sleep 2; curl -f http://localhost:3001/health && kill $PID)</verify>
  <done>Hono server is running and exporting its type definition.</done>
</task>

</tasks>

<verification>
Verify server responds correctly and types are exported.
</verification>

<success_criteria>
Hono API is operational and type-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
