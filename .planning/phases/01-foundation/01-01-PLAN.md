---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pnpm-workspace.yaml, turbo.json, package.json, packages/tsconfig/base.json, packages/eslint-config/base.js]
autonomous: true

must_haves:
  truths:
    - "pnpm workspace is active and recognizes packages directory"
    - "Turborepo can run a 'build' task across the workspace"
    - "Shared TS and ESLint configs are importable by other packages"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace definition"
    - path: "turbo.json"
      provides: "Task orchestration"
    - path: "packages/tsconfig/package.json"
      provides: "Shared TS config package"
    - path: "packages/eslint-config/package.json"
      provides: "Shared ESLint config package"
---

<objective>
Initialize the monorepo structure using pnpm workspaces and Turborepo, and establish shared configuration packages for TypeScript and ESLint.

Purpose: Provides the infrastructure for high-performance builds and consistent code standards.
Output: Monorepo root configuration and base shared packages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize pnpm workspace and Turborepo</name>
  <files>pnpm-workspace.yaml, turbo.json, package.json</files>
  <action>
    Initialize the root package.json with basic metadata and a pnpm-workspace.yaml defining 'apps/*' and 'packages/*'.
    Create turbo.json with 'build', 'dev', and 'lint' task definitions, ensuring 'build' depends on '^build'.
  </action>
  <verify>pnpm install && npx turbo build</verify>
  <done>Root workspace is configured and turbo is ready to orchestrate tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Setup shared configuration packages</name>
  <files>packages/tsconfig/base.json, packages/tsconfig/package.json, packages/eslint-config/base.js, packages/eslint-config/package.json</files>
  <action>
    Create 'packages/tsconfig' with a base.json containing strict TS rules.
    Create 'packages/eslint-config' with a base.js implementing Airbnb-style rules using @eslint/compat for ESLint v9 Flat Config support.
    Ensure both have valid package.json files with appropriate 'exports'.
  </action>
  <verify>Test files in sub-packages can extend these configs without errors.</verify>
  <done>Shared configurations are available as workspace dependencies.</done>
</task>

</tasks>

<verification>
Check that `pnpm m ls` shows the new packages and `turbo lint` runs without config errors.
</verification>

<success_criteria>
Monorepo root and shared configs are committed and functional.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
