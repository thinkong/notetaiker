---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pnpm-workspace.yaml, turbo.json, package.json, packages/tsconfig/base.json, packages/tsconfig/package.json, packages/eslint-config/base.js, packages/eslint-config/package.json, .env.example, packages/env/package.json, packages/env/index.ts]
autonomous: true

must_haves:
  truths:
    - "pnpm workspace is active and recognizes packages directory"
    - "Turborepo can run a 'build' task across the workspace"
    - "The codebase maintains consistent formatting and type safety across all packages via centralized rules"
    - "Environment variable validation pattern is established"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace definition"
    - path: "turbo.json"
      provides: "Task orchestration"
    - path: "packages/tsconfig/package.json"
      provides: "Shared TS config package"
    - path: "packages/eslint-config/package.json"
      provides: "Shared ESLint config package"
    - path: ".env.example"
      provides: "Environment template"
    - path: "packages/env/index.ts"
      provides: "Type-safe environment validation"
---

<objective>
Initialize the monorepo structure using pnpm workspaces and Turborepo, establish shared configuration packages for TypeScript and ESLint, and setup a type-safe environment variable validation pattern.

Purpose: Provides the infrastructure for high-performance builds, consistent code standards, and reliable configuration management.
Output: Monorepo root configuration, base shared packages, and environment validation utility.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize pnpm workspace and Turborepo</name>
  <files>pnpm-workspace.yaml, turbo.json, package.json, .env.example</files>
  <action>
    Initialize the root package.json with basic metadata and a pnpm-workspace.yaml defining 'apps/*' and 'packages/*'.
    Create turbo.json with 'build', 'dev', and 'lint' task definitions, ensuring 'build' depends on '^build'.
    Add a .env.example with basic placeholders (e.g., NODE_ENV=development).
  </action>
  <verify>pnpm install && npx turbo build</verify>
  <done>Root workspace is configured and turbo is ready to orchestrate tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Setup shared configuration packages</name>
  <files>packages/tsconfig/base.json, packages/tsconfig/package.json, packages/eslint-config/base.js, packages/eslint-config/package.json</files>
  <action>
    Create 'packages/tsconfig' with a base.json containing strict TS rules and name the package '@notetaiker/tsconfig'.
    Create 'packages/eslint-config' with a base.js implementing Airbnb-style rules using @eslint/compat for ESLint v9 Flat Config support, and name the package '@notetaiker/eslint-config'.
    Ensure both have valid package.json files with appropriate 'exports'.
  </action>
  <verify>pnpm -F "@notetaiker/*" exec ls</verify>
  <done>Shared configurations are available as workspace dependencies.</done>
</task>

<task type="auto">
  <name>Task 3: Establish environment variable validation</name>
  <files>packages/env/package.json, packages/env/index.ts</files>
  <action>
    Create a shared '@notetaiker/env' package.
    Use 'zod' to define a schema for environment variables (starting with NODE_ENV).
    Export a validated 'env' object that throws informative errors if variables are missing or invalid.
  </action>
  <verify>pnpm -F "@notetaiker/env" exec ls</verify>
  <done>Type-safe environment validation is ready for use in apps.</done>
</task>

</tasks>

<verification>
Check that `pnpm m ls` shows the new packages and `turbo lint` runs without config errors.
</verification>

<success_criteria>
Monorepo root and shared configs are committed and functional.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
