---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  [
    package.json,
    .husky/pre-commit,
    .husky/commit-msg,
    .changeset/config.json,
    commitlint.config.js,
  ]
autonomous: true

must_haves:
  truths:
    - "Conventional commits are enforced on commit"
    - "Changesets can be initialized for a release"
  artifacts:
    - path: ".husky/commit-msg"
      provides: "Commit linting hook"
    - path: ".changeset/config.json"
      provides: "Versioning configuration"
---

<objective>
Configure development tooling for quality assurance, including git hooks for linting/commits and Changesets for versioning.

Purpose: Enforce project standards and automate the release/versioning workflow.
Output: Husky hooks, Commitlint, and Changesets configuration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Husky and Commitlint</name>
  <files>.husky/pre-commit, .husky/commit-msg, package.json, commitlint.config.js</files>
  <action>
    Install husky, @commitlint/cli, and @commitlint/config-conventional.
    Initialize husky and add a 'commit-msg' hook that runs commitlint.
    Add a 'pre-commit' hook that runs 'turbo lint'.
    Create commitlint.config.js extending '@commitlint/config-conventional'.
  </action>
  <verify>echo "foo" | npx commitlint && exit 1 || exit 0</verify>
  <done>Quality gates are enforced at the git level.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Changesets</name>
  <files>.changeset/config.json</files>
  <action>
    Install @changesets/cli.
    Run 'npx changeset init' and configure it for the monorepo structure.
  </action>
  <verify>npx changeset add creates a new changeset file.</verify>
  <done>Versioning workflow is established.</done>
</task>

</tasks>

<verification>
Confirm git hooks block invalid commits and changesets can be created.
</verification>

<success_criteria>
Project quality gates and versioning tools are fully configured.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
