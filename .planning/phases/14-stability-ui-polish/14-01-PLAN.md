---
phase: 14-stability-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  [
    apps/web/src/components/preview/NotePreviewOverlay.tsx,
    apps/api/src/routes/notes.ts,
  ]
autonomous: false
must_haves:
  truths:
    - "Note preview displays both manual and AI tags"
    - "Manual and AI tags are visually distinguished using the Tag component"
    - "Manual tags are prioritized and shown before AI tags"
    - "Clicking 'x' on a tag triggers a confirmation dialog"
    - "Backend supports updating note metadata via PATCH"
  artifacts:
    - path: "apps/web/src/components/preview/NotePreviewOverlay.tsx"
      provides: "Unified tag rendering and deletion workflow"
    - path: "apps/api/src/routes/notes.ts"
      provides: "Endpoint for updating note metadata"
---

<objective>
Unify the tag UI in the Note Preview Overlay to display both manual and AI tags using the standard Tag component, and implement the tag removal workflow with confirmation by adding a backend update endpoint.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/components/preview/NotePreviewOverlay.tsx
@apps/web/src/components/common/Tag.tsx
@apps/web/src/components/common/ConfirmDialog.tsx
@apps/api/src/routes/notes.ts
</context>

<tasks>

<task type="auto">
  <name>Implement Patch Note Metadata Endpoint</name>
  <files>apps/api/src/routes/notes.ts</files>
  <action>
    - Add a PATCH `/:id` endpoint to `apps/api/src/routes/notes.ts`.
    - The endpoint should validate the body using Zod (allowing `metadata` update).
    - It should retrieve the existing note, merge the new metadata, and call `storageService.saveNote`.
    - Ensure it triggers re-indexing by calling `queueService.enqueue(id)`.
  </action>
  <verify>
    - Send a PATCH request to `/api/notes/:id` with updated tags.
    - Verify the note file on disk reflects the change.
  </verify>
  <done>
    - Backend supports partial metadata updates for notes.
  </done>
</task>

<task type="auto">
  <name>Refactor Tag Rendering</name>
  <files>apps/web/src/components/preview/NotePreviewOverlay.tsx</files>
  <action>
    - Import the `Tag` component from `../common/Tag`.
    - Replace the comma-separated tag list with a flex container (`flex flex-wrap gap-2`).
    - Render manual tags (`note.metadata.tags`) using `variant="manual"`.
    - Render AI tags (`note.metadata.ai_tags`) using `variant="ai"`.
    - Ensure manual tags appear before AI tags.
    - If a tag exists in both lists, render it once as a manual tag.
    - Implement overflow handling: if tags exceed one line, show a "+X more" indicator.
  </action>
  <verify>
    - Open Note Preview Overlay.
    - Verify manual and AI tags are visible and styled correctly.
  </verify>
  <done>
    - Tag component is used for all tags in the preview overlay.
  </done>
</task>

<task type="auto">
  <name>Implement Tag Removal with Confirmation</name>
  <files>apps/web/src/components/preview/NotePreviewOverlay.tsx</files>
  <action>
    - Import `ConfirmDialog` and `useState`.
    - Add state to track the tag being deleted and the confirmation dialog visibility.
    - Pass `onDismiss` to `Tag` components in the preview.
    - When `onDismiss` is called, show the `ConfirmDialog`.
    - On confirmation, call the new PATCH API to update the note (removing the tag from either `tags` or `ai_tags`).
    - Use TanStack Query's `useMutation` to handle the update and invalidate the `["note", noteId]` query.
  </action>
  <verify>
    - Click 'x' on a tag in the preview.
    - Verify confirmation dialog appears.
    - Confirm deletion and verify tag is removed from the UI and persisted.
  </verify>
  <done>
    - Tags can be removed from the preview overlay after confirmation.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Unified tag display and removal workflow in Note Preview Overlay.</what-built>
  <how-to-verify>
    1. Open a note with both manual and AI tags in the preview overlay.
    2. Verify visual distinction between tag types.
    3. Click 'x' on a tag and verify the confirmation dialog works.
    4. Confirm deletion and check if the tag is gone.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- Tag component used in NotePreviewOverlay.
- Both manual and AI tags are displayed.
- Deletion workflow includes confirmation and backend persistence.
</verification>

<success_criteria>

- Visual consistency for tags across the app.
- Secure tag removal process with backend synchronization.
  </success_criteria>

<output>
After completion, create `.planning/phases/14-stability-ui-polish/14-01-SUMMARY.md`
</output>
