---
phase: 14-stability-ui-polish
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified: [packages/tsconfig/base.json, apps/web/tsconfig.app.json, packages/eslint-config/base.js, apps/api/eslint.config.js, apps/web/eslint.config.js]
autonomous: true
must_haves:
  truths:
    - "Project builds successfully with verbatimModuleSyntax enabled in base TSConfig"
    - "Linter enforces 'import type' for type-only imports"
    - "No build regressions due to ghost imports"
  artifacts:
    - path: "packages/tsconfig/base.json"
      contains: '"verbatimModuleSyntax": true'
    - path: "packages/eslint-config/base.js"
      contains: "@typescript-eslint/consistent-type-imports"
---

<objective>
Harden the build system by enabling verbatimModuleSyntax in the base TypeScript configuration and enforcing proper type import syntax via ESLint across the monorepo.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/tsconfig/base.json
@apps/web/tsconfig.app.json
@packages/eslint-config/base.js
@apps/api/eslint.config.js
@apps/web/eslint.config.js
</context>

<tasks>

<task type="auto">
  <name>Enable verbatimModuleSyntax</name>
  <files>packages/tsconfig/base.json, apps/web/tsconfig.app.json</files>
  <action>
    - Add `"verbatimModuleSyntax": true` to `packages/tsconfig/base.json`.
    - Remove `"verbatimModuleSyntax": true` from `apps/web/tsconfig.app.json` as it is now inherited from base.
    - Run `pnpm build` across the monorepo.
    - Manually fix any resulting errors (e.g., in `apps/api/src/services/worker.service.test.ts` as noted in research) by converting value imports to `import type` where appropriate.
  </action>
  <verify>
    - `pnpm build` completes without errors in both `apps/api` and `apps/web`.
  </verify>
  <done>
    - `verbatimModuleSyntax` is active and the build is clean.
  </done>
</task>

<task type="auto">
  <name>Enforce Type Imports in Linter</name>
  <files>packages/eslint-config/base.js, apps/api/eslint.config.js, apps/web/eslint.config.js</files>
  <action>
    - Add the `@typescript-eslint/consistent-type-imports` rule to `packages/eslint-config/base.js` (or specifically in the TS-matching files sections of the apps).
    - Configuration:
      ```javascript
      "@typescript-eslint/consistent-type-imports": [
        "error",
        {
          prefer: "type-imports",
          fixStyle: "separate-type-imports",
        },
      ]
      ```
    - Ensure `typescript-eslint` is correctly configured to handle these rules.
    - Run `pnpm lint:fix` to automatically update imports across the codebase.
  </action>
  <verify>
    - `pnpm lint` passes without errors.
    - Check a few files to ensure `import type` is being used correctly for types.
  </verify>
  <done>
    - Linter enforces `verbatimModuleSyntax` compliant imports.
  </done>
</task>

</tasks>

<verification>
- Build passes with strict module syntax.
- Lint passes with type import enforcement.
</verification>

<success_criteria>
- Zero build regressions from type-only imports.
- Standardized import style across the monorepo.
</success_criteria>

<output>
After completion, create `.planning/phases/14-stability-ui-polish/14-02-SUMMARY.md`
</output>
