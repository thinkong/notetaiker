---
phase: 04-timeline-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - apps/web/package.json
  - apps/web/src/App.tsx
  - apps/web/src/hooks/useTimeline.ts
autonomous: true

must_haves:
  truths:
    - "React application is wrapped in QueryClientProvider"
    - "useTimeline hook correctly fetches first page of notes"
  artifacts:
    - path: "apps/web/src/hooks/useTimeline.ts"
      provides: "Infinite query hook for notes"
    - path: "apps/web/src/App.tsx"
      provides: "QueryClient initialization"
  key_links:
    - from: "apps/web/src/hooks/useTimeline.ts"
      to: "/api/notes"
      via: "useInfiniteQuery with fetch"
---

<objective>
Setup TanStack Query and implement the core data fetching hook for the Timeline.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/web/package.json
@apps/web/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Install dependencies and setup QueryClient</name>
  <files>apps/web/package.json, apps/web/src/App.tsx</files>
  <action>
    Install @tanstack/react-query, react-intersection-observer, and date-fns in apps/web.
    Initialize a new QueryClient in apps/web/src/App.tsx and wrap the application with QueryClientProvider.
  </action>
  <verify>
    pnpm --filter @notetaiker/web dev runs without errors.
  </verify>
  <done>
    Dependencies are installed and TanStack Query is configured in the root component.
  </done>
</task>

<task type="auto">
  <name>Implement useTimeline hook</name>
  <files>apps/web/src/hooks/useTimeline.ts</files>
  <action>
    Create useTimeline.ts using TanStack Query's useInfiniteQuery.
    It should fetch paginated notes from GET /api/notes?limit=X&offset=Y.
    Implement getNextPageParam logic based on the number of notes returned vs the limit.
  </action>
  <verify>
    Check network tab in dev tools to ensure /api/notes is called with correct parameters.
  </verify>
  <done>
    useTimeline hook is ready for use in components, correctly handling pagination state.
  </done>
</task>

</tasks>

<verification>
Verify hook correctly fetches data from the API and manages infinite scroll state.
</verification>

<success_criteria>
Frontend is configured for data fetching and can retrieve paginated notes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-timeline-ui/04-02-SUMMARY.md`
</output>
