---
phase: 04-timeline-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - apps/web/src/components/timeline/NoteCard.tsx
  - apps/web/src/components/timeline/SkeletonCard.tsx
  - apps/web/src/components/timeline/Timeline.tsx
  - apps/web/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "User can see a list of notes in the UI"
    - "Scrolling to the bottom loads more notes"
    - "Note cards show relative timestamps"
    - "Long notes are truncated and expandable"
  artifacts:
    - path: "apps/web/src/components/timeline/Timeline.tsx"
      provides: "Infinite scroll feed"
    - path: "apps/web/src/components/timeline/NoteCard.tsx"
      provides: "Note visualization with expansion"
  key_links:
    - from: "apps/web/src/components/timeline/Timeline.tsx"
      to: "apps/web/src/hooks/useTimeline.ts"
      via: "hook call and data mapping"
    - from: "apps/web/src/App.tsx"
      to: "apps/web/src/components/timeline/Timeline.tsx"
      via: "component rendering"
---

<objective>
Implement the frontend components for the Timeline UI, including infinite scroll logic, note cards with "expand-in-place" functionality, and relative timestamps.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/web/src/hooks/useTimeline.ts
@apps/web/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Create NoteCard and SkeletonCard components</name>
  <files>apps/web/src/components/timeline/NoteCard.tsx, apps/web/src/components/timeline/SkeletonCard.tsx</files>
  <action>
    Create NoteCard.tsx to display note content, title (if any), and relative timestamp (using date-fns formatDistanceToNow).
    Implement line-clamping (e.g., 3 lines) for long notes with a "Show more" button that expands the card in place.
    Style using Nord theme colors and Tailwind.
    Create SkeletonCard.tsx for loading states.
  </action>
  <verify>
    Visual check of components (can be done in isolation or via Timeline).
  </verify>
  <done>
    NoteCard and SkeletonCard components are styled and functional.
  </done>
</task>

<task type="auto">
  <name>Create Timeline component with infinite scroll</name>
  <files>apps/web/src/components/timeline/Timeline.tsx</files>
  <action>
    Create Timeline.tsx that consumes the useTimeline hook.
    Map over the flattened pages of notes and render NoteCard components.
    Implement infinite scroll using react-intersection-observer: place an invisible 'sentinel' div at the bottom that triggers fetchNextPage when visible.
    Show SkeletonCards while loading initial data or the next page.
  </action>
  <verify>
    Scroll to the bottom of the timeline and verify more notes load and appear.
  </verify>
  <done>
    Timeline component correctly displays notes and handles infinite scrolling.
  </done>
</task>

<task type="auto">
  <name>Integrate Timeline into Main Layout</name>
  <files>apps/web/src/App.tsx</files>
  <action>
    Add the Timeline component to the main view in apps/web/src/App.tsx.
    Position it below the NoteEditor component.
    Ensure layout is responsive and handles the dual-section view correctly (Editor at top/side, Timeline taking remaining space).
  </action>
  <verify>
    Open the web app and verify the timeline appears and is usable.
  </verify>
  <done>
    Timeline is successfully integrated into the application layout.
  </done>
</task>

</tasks>

<verification>
Verify full end-to-end flow: open app, see notes, scroll to load more, expand a long note.
</verification>

<success_criteria>
User can interactively browse their notes history via a smooth, infinite-scrolling timeline.
</success_criteria>

<output>
After completion, create `.planning/phases/04-timeline-ui/04-03-SUMMARY.md`
</output>
