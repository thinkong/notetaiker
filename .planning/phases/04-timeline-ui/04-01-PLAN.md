---
phase: 04-timeline-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/services/storage.service.ts
  - apps/api/src/routes/notes.ts
  - apps/api/src/services/storage.service.test.ts
autonomous: true

must_haves:
  truths:
    - "API returns paginated notes sorted by most recent first"
    - "API respects limit and offset query parameters"
  artifacts:
    - path: "apps/api/src/services/storage.service.ts"
      provides: "Paginated file listing"
    - path: "apps/api/src/routes/notes.ts"
      provides: "Paginated API endpoint"
  key_links:
    - from: "apps/api/src/routes/notes.ts"
      to: "apps/api/src/services/storage.service.ts"
      via: "listNotes call with limit/offset"
---

<objective>
Update the Storage Service and API routes to support paginated retrieval of notes, enabling efficient infinite scroll in the frontend.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/api/src/services/storage.service.ts
@apps/api/src/routes/notes.ts
</context>

<tasks>

<task type="auto">
  <name>Add pagination to StorageService.listNotes</name>
  <files>apps/api/src/services/storage.service.ts</files>
  <action>
    Update listNotes in apps/api/src/services/storage.service.ts to accept optional limit and offset parameters.
    The method should retrieve all note metadata, sort them by timestamp (descending), and return the requested slice.
    Ensure it handles cases where limit or offset are larger than the total count.
  </action>
  <verify>
    Run pnpm --filter @notetaiker/api test
  </verify>
  <done>
    listNotes(limit, offset) returns correctly sliced and sorted array of NoteMetadata.
  </done>
</task>

<task type="auto">
  <name>Update Hono notes route for pagination</name>
  <files>apps/api/src/routes/notes.ts</files>
  <action>
    Update the GET /api/notes route in apps/api/src/routes/notes.ts to accept limit and offset query parameters.
    Validate parameters with Zod (default limit=50, offset=0).
    Pass these parameters to storageService.listNotes().
  </action>
  <verify>
    curl "http://localhost:3000/api/notes?limit=2&offset=0" returns the 2 most recent notes.
  </verify>
  <done>
    API endpoint supports pagination and returns 200 OK with the expected data slice.
  </done>
</task>

<task type="auto">
  <name>Add tests for paginated notes retrieval</name>
  <files>apps/api/src/services/storage.service.test.ts</files>
  <action>
    Add unit tests in apps/api/src/services/storage.service.test.ts to verify that listNotes correctly handles limit and offset.
    Include cases for: first page, middle page, and out-of-bounds offset.
  </action>
  <verify>
    pnpm --filter @notetaiker/api test
  </verify>
  <done>
    All storage service tests pass with 100% coverage of pagination logic.
  </done>
</task>

</tasks>

<verification>
Verify API pagination via curl and automated tests.
</verification>

<success_criteria>
API successfully serves paginated notes sorted by date.
</success_criteria>

<output>
After completion, create `.planning/phases/04-timeline-ui/04-01-SUMMARY.md`
</output>
