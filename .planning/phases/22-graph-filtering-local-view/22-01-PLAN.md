---
phase: 22-graph-filtering-local-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/contexts/GraphStateContext.tsx
  - apps/web/src/components/graph/ForceGraph.tsx
autonomous: true
must_haves:
  truths:
    - "Nodes not matching filter tags appear dimmed (reduced opacity)"
    - "Nodes outside 1-hop local view appear dimmed"
    - "Alt + Double-click on a node activates local view focused on that node"
    - "Clicking a neighbor in local view shifts focus to that neighbor"
  artifacts:
    - path: "apps/web/src/contexts/GraphStateContext.tsx"
      provides: "Filter and Local View state management"
    - path: "apps/web/src/components/graph/ForceGraph.tsx"
      provides: "Render-level ghosting and local view navigation"
  key_links:
    - from: "apps/web/src/components/graph/ForceGraph.tsx"
      to: "apps/web/src/contexts/GraphStateContext.tsx"
      via: "useGraphState hook"
---

<objective>
Establish the state and rendering logic for graph filtering and local view.
Purpose: Enable visual focus on subsets of the graph without disrupting the global layout.
Output: Updated context state and a canvas renderer that supports opacity-based "ghosting".
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/src/contexts/GraphStateContext.tsx
@apps/web/src/components/graph/ForceGraph.tsx
</context>

<tasks>

<task type="auto">
  <name>Update GraphStateContext</name>
  <files>apps/web/src/contexts/GraphStateContext.tsx</files>
  <action>
    Add the following state to GraphStateContext:
    - `filterTags: string[]`
    - `filterLogic: 'AND' | 'OR'`
    - `localNodeId: string | null` (for local view center)
    - `setFilterTags`, `setFilterLogic`, `setLocalNodeId` actions.
    Ensure initial state is empty/null.
  </action>
  <verify>Check that context exports the new state and setter functions.</verify>
  <done>GraphStateContext includes filter and local view state with appropriate setters.</done>
</task>

<task type="auto">
  <name>Update ForceGraph rendering logic</name>
  <files>apps/web/src/components/graph/ForceGraph.tsx</files>
  <action>
    Implement visibility calculation in the render loop:
    - Calculate `isGhosted` for each node:
      - If `filterTags` is not empty: node must match tags based on `filterLogic`.
      - If `localNodeId` is active: node must be the center or a 1-hop neighbor.
    - Set node and link opacity to 0.15 if ghosted, 1.0 otherwise.
    - Maintain full interactivity for ghosted nodes.
    - Use a Set for O(1) lookup of visible node IDs during rendering.
  </action>
  <verify>Manual verification that nodes can be visually dimmed via state changes (can test by temporarily hardcoding state).</verify>
  <done>ForceGraph renders ghosted nodes/links with reduced opacity while keeping them interactive.</done>
</task>

<task type="auto">
  <name>Implement Alt + Double-click for Local View</name>
  <files>apps/web/src/components/graph/ForceGraph.tsx</files>
  <action>
    Update `onNodeDoubleClick` or the click handling logic:
    - Detect if `event.altKey` is pressed.
    - If Alt + Double-click: Call `setLocalNodeId(node.id)` and center the camera on the node.
    - If standard Double-click: Continue existing behavior (opening the note).
  </action>
  <verify>Double-click a node while holding Alt; verify (via logs) that it triggers local view mode.</verify>
  <done>Alt + Double-click toggles local view for the clicked node.</done>
</task>

<task type="auto">
  <name>Implement "Walking the Graph" in Local View</name>
  <files>apps/web/src/components/graph/ForceGraph.tsx</files>
  <action>
    When `localNodeId` is active:
    - Single-clicking a neighbor node (which is visible in local view) should call `setLocalNodeId(neighbor.id)`.
    - This should trigger the camera to center on the new node and update the 1-hop visibility set.
  </action>
  <verify>Enter local view, click a neighbor, and verify focus shifts to that neighbor.</verify>
  <done>User can navigate through the graph in local view mode by clicking visible neighbors.</done>
</task>

</tasks>

<success_criteria>

1. Graph State supports filtering and local view.
2. Canvas renders ghosted nodes with reduced opacity.
3. Alt + Double-click enters local view and centers camera.
4. Clicking neighbors "walks" the local graph.
   </success_criteria>
