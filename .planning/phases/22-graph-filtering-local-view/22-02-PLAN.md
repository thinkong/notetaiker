---
phase: 22-graph-filtering-local-view
plan: 02
type: execute
wave: 2
depends_on: ["22-01-PLAN.md"]
files_modified:
  - apps/web/src/components/graph/GraphToolbar.tsx
  - apps/web/src/components/graph/GraphFilterChips.tsx
autonomous: true
must_haves:
  truths:
    - "User can search for tags using a command-palette style input"
    - "User can toggle between 'Match ANY' (OR) and 'Match ALL' (AND) logic"
    - "Active filters appear as removable chips in the UI"
  artifacts:
    - path: "apps/web/src/components/graph/GraphToolbar.tsx"
      provides: "Tag search and logic toggle controls"
    - path: "apps/web/src/components/graph/GraphFilterChips.tsx"
      provides: "Display and removal of active tag filters"
  key_links:
    - from: "apps/web/src/components/graph/GraphToolbar.tsx"
      to: "apps/web/src/contexts/GraphStateContext.tsx"
      via: "setFilterTags/setFilterLogic"
---

<objective>
Create the UI components for managing graph filters and local view.
Purpose: Provide an intuitive interface for users to reduce noise in the graph.
Output: Standalone Toolbar and FilterChips components.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/src/contexts/GraphStateContext.tsx
@apps/web/src/components/search/SearchPalette.tsx
</context>

<tasks>

<task type="auto">
  <name>Create GraphToolbar component</name>
  <files>apps/web/src/components/graph/GraphToolbar.tsx</files>
  <action>
    Implement a toolbar that includes:
    - A tag search input using `cmdk` (consistent with `SearchPalette`).
    - A toggle for `filterLogic` (AND/OR).
    - A button to clear `localNodeId` if active (displaying "Local View: [Node Title]").
    - Ensure it uses Tailwind for styling and matches the app's aesthetic.
  </action>
  <verify>Component exists and exports logic for tag selection and toggle.</verify>
  <done>GraphToolbar provides controls for filtering and local view management.</done>
</task>

<task type="auto">
  <name>Create GraphFilterChips component</name>
  <files>apps/web/src/components/graph/GraphFilterChips.tsx</files>
  <action>
    Implement a component that:
    - Renders `filterTags` as a list of chips.
    - Each chip has a 'close' icon that removes the tag from `filterTags` on click.
    - Animates entry/exit of chips if possible using `framer-motion`.
  </action>
  <verify>Component correctly renders a list of tags and triggers removal on click.</verify>
  <done>GraphFilterChips allows users to see and remove active filters.</done>
</task>

</tasks>

<success_criteria>
1. Toolbar allows searching/adding tags.
2. Toolbar allows toggling AND/OR logic.
3. Filter chips correctly reflect and modify state.
</success_criteria>
