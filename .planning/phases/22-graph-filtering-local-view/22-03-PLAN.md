---
phase: 22-graph-filtering-local-view
plan: 03
type: execute
wave: 3
depends_on: ["22-02-PLAN.md"]
files_modified:
  - apps/web/src/components/graph/GraphView.tsx
autonomous: true
must_haves:
  truths:
    - "Selecting a ghosted node (e.g. from Sidebar) clears all filters to reveal it"
    - "The graph background color changes slightly when filters are active"
    - "A 'No matching notes' message appears if filters yield zero results"
  artifacts:
    - path: "apps/web/src/components/graph/GraphView.tsx"
      provides: "Layout integration and global behavior coordination"
  key_links:
    - from: "apps/web/src/components/graph/GraphView.tsx"
      to: "apps/web/src/components/graph/GraphToolbar.tsx"
      via: "React component inclusion"
    - from: "apps/web/src/components/graph/GraphView.tsx"
      to: "apps/web/src/contexts/SelectionContext.tsx"
      via: "Automatic filter clearing logic"
---

<objective>
Integrate UI components into the Graph View and implement global behaviors.
Purpose: Complete the feature by coordinating UI, background state, and external interactions.
Output: Fully functional Graph Filtering and Local View experience.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/src/components/graph/GraphView.tsx
@apps/web/src/components/graph/GraphToolbar.tsx
@apps/web/src/components/graph/GraphFilterChips.tsx
</context>

<tasks>

<task type="auto">
  <name>Integrate Toolbar into GraphView</name>
  <files>apps/web/src/components/graph/GraphView.tsx</files>
  <action>
    - Add `GraphToolbar` to the top of the `GraphView` layout.
    - Add `GraphFilterChips` below the toolbar or in a floating container.
    - Update the canvas container to account for toolbar height.
    - Implement background tinting on the canvas container when `filterTags` or `localNodeId` is active.
  </action>
  <verify>UI components are visible in Graph View; background tints when filters are applied.</verify>
  <done>GraphView integrates controls and visual feedback for active filtering.</done>
</task>

<task type="auto">
  <name>Implement External Selection filter clearing</name>
  <files>apps/web/src/components/graph/GraphView.tsx</files>
  <action>
    - Listen for changes to the globally selected node (from `SelectionContext`).
    - If a node is selected and it is currently ghosted (filtered out or not in local view):
      - Clear `filterTags`.
      - Clear `localNodeId`.
    - This ensures that if a user clicks a note in the sidebar, the graph reveals it.
  </action>
  <verify>Apply a filter, then select a hidden note from the sidebar; verify filters clear automatically.</verify>
  <done>System automatically reveals selected nodes by clearing active filters.</done>
</task>

<task type="auto">
  <name>Add Empty State overlay</name>
  <files>apps/web/src/components/graph/GraphView.tsx</files>
  <action>
    - Check if any nodes are currently "visible" (not ghosted).
    - If `filterTags` is active and zero nodes match:
      - Render a centered overlay message: "No matching notes found with active filters."
  </action>
  <verify>Apply a filter that matches no nodes; verify the message appears.</verify>
  <done>User receives feedback when filters result in an empty graph.</done>
</task>

</tasks>

<success_criteria>
1. Filtering UI is fully integrated into Graph View.
2. Selecting a hidden node clears filters automatically.
3. Visual feedback (tinting, empty state) is provided for active filtering.
</success_criteria>
