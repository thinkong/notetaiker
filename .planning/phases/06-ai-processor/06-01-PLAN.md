---
phase: 06-ai-processor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  [
    package.json,
    apps/api/package.json,
    apps/api/src/index.ts,
    apps/api/src/services/queue.service.ts,
  ]
autonomous: true

must_haves:
  truths:
    - "Jobs can be persisted to a local SQLite database"
    - "Jobs survive application restarts"
    - "Stuck 'processing' jobs are recovered on startup"
  artifacts:
    - path: "apps/api/src/services/queue.service.ts"
      provides: "SQLite-backed job queue management"
  key_links:
    - from: "apps/api/src/services/queue.service.ts"
      to: ".notetaiker/queue.db"
      via: "better-sqlite3"
---

<objective>
Establish the persistent storage layer for background AI jobs using SQLite. This ensures that processing tasks are not lost if the application is closed or crashes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-processor/06-RESEARCH.md
@.planning/phases/06-ai-processor/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install queue dependencies</name>
  <files>package.json, apps/api/package.json</files>
  <action>
    Install required libraries for the queue and persistence:
    - better-sqlite3 (persistence)
    - p-queue (concurrency)
    - p-retry (exponential backoff)
    - @types/better-sqlite3 (dev dependency)

    Add them to the workspace root and/or apps/api as appropriate for the monorepo structure.

  </action>
  <verify>pnpm list better-sqlite3 p-queue p-retry</verify>
  <done>Dependencies are installed and available in the API package.</done>
</task>

<task type="auto">
  <name>Task 2: Implement QueueService with SQLite</name>
  <files>apps/api/src/services/queue.service.ts</files>
  <action>
    Create a `QueueService` that:
    1. Initializes a SQLite database in `.notetaiker/queue.db` (sharing the same storage root as secrets).
    2. Creates a `jobs` table: `id` (UUID), `noteId` (TEXT), `status` ('queued', 'processing', 'completed', 'failed'), `attempts` (INTEGER), `lastError` (TEXT), `createdAt` (DATETIME), `updatedAt` (DATETIME).
    3. Provides methods: `enqueue(noteId)`, `getNextJob()`, `updateJobStatus(id, status, error?)`, `resetProcessingJobs()`.
  </action>
  <verify>Create a small test script or use a Vitest test to verify jobs can be inserted and retrieved from the DB.</verify>
  <done>QueueService can persist and transition job states in SQLite.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Startup Recovery</name>
  <files>apps/api/src/index.ts, apps/api/src/services/queue.service.ts</files>
  <action>
    Integrate `queueService.resetProcessingJobs()` into the API startup sequence in `index.ts`. This must find any jobs that were stuck in 'processing' state when the app closed and reset them to 'queued' so they can be picked up by the worker.
  </action>
  <verify>Manually insert a 'processing' job into the DB, restart the API, and verify it is now 'queued'.</verify>
  <done>Application recovers interrupted jobs on startup.</done>
</task>

</tasks>

<verification>
- Run `sqlite3 .notetaiker/queue.db ".tables"` to verify table creation.
- Check API logs for successful queue initialization.
</verification>

<success_criteria>
Jobs are successfully written to SQLite and state transitions are persistent.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-processor/06-01-SUMMARY.md`
</output>
