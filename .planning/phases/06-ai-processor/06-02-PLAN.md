---
phase: 06-ai-processor
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [apps/api/src/services/worker.service.ts, apps/api/src/index.ts, apps/api/src/routes/notes.ts]
autonomous: true

must_haves:
  truths:
    - "Background worker processes queued jobs with limited concurrency"
    - "Failed jobs are retried with exponential backoff"
    - "Processing doesn't block the main API response"
  artifacts:
    - path: "apps/api/src/services/worker.service.ts"
      provides: "p-queue based job orchestration"
  key_links:
    - from: "apps/api/src/services/worker.service.ts"
      to: "apps/api/src/services/queue.service.ts"
      via: "polling/event triggers"
---

<objective>
Implement the background worker orchestrator using p-queue and p-retry. This service will manage the execution of jobs from the SQLite queue with controlled concurrency and error handling.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-ai-processor/06-RESEARCH.md
@.planning/phases/06-ai-processor/06-CONTEXT.md
@.planning/phases/06-ai-processor/06-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WorkerService</name>
  <files>apps/api/src/services/worker.service.ts</files>
  <action>
    Create a `WorkerService` that:
    1. Initializes `PQueue` with `concurrency: 2` (as per CONTEXT.md).
    2. Implements a `start()` method that polls `QueueService` for pending jobs or listens for new job events.
    3. Implements `processJob(jobId)` which:
       - Uses `p-retry` for the execution logic (3-5 retries).
       - Updates job status in SQLite via `QueueService`.
       - For now, uses a "stub" processor that sleeps and logs (real AI logic is Phase 7).
  </action>
  <verify>Log output shows worker picking up jobs and simulating processing.</verify>
  <done>WorkerService can orchestrate job execution from the queue.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Worker with API</name>
  <files>apps/api/src/index.ts, apps/api/src/routes/notes.ts</files>
  <action>
    1. Initialize and start `WorkerService` in `apps/api/src/index.ts`.
    2. Modify the note creation/update route in `apps/api/src/routes/notes.ts` to call `queueService.enqueue(noteId)` whenever a note is saved.
    3. Ensure the enqueue call is "fire and forget" or happens after the response is sent to avoid blocking the user.
  </action>
  <verify>Create a note via the API and verify a corresponding job appears in the queue and is processed by the worker logs.</verify>
  <done>Note operations automatically trigger background background jobs.</done>
</task>

</tasks>

<verification>
- Verify that creating multiple notes rapidly doesn't block the API.
- Verify that only 2 jobs are "processed" at a time in the logs.
</verification>

<success_criteria>
Notes created via the API are automatically enqueued and processed by the background worker.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-processor/06-02-SUMMARY.md`
</output>
