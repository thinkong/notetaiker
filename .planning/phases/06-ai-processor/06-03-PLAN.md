---
phase: 06-ai-processor
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: [apps/api/src/routes/events.ts, apps/api/src/index.ts, apps/web/src/hooks/useTimeline.ts, apps/web/src/components/NoteCard.tsx]
autonomous: true

must_haves:
  truths:
    - "UI updates automatically when background processing completes"
    - "AI tags are visually distinct from manual tags"
    - "SSE connection is cleaned up on client disconnect"
  artifacts:
    - path: "apps/api/src/routes/events.ts"
      provides: "SSE endpoint for real-time updates"
  key_links:
    - from: "apps/web/src/hooks/useTimeline.ts"
      to: "/api/events"
      via: "EventSource"
---

<objective>
Implement the real-time update mechanism using Server-Sent Events (SSE) and update the frontend UI to display AI-generated tags with a distinct visual style.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-ai-processor/06-RESEARCH.md
@.planning/phases/06-ai-processor/06-CONTEXT.md
@.planning/phases/06-ai-processor/06-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SSE Endpoint</name>
  <files>apps/api/src/routes/events.ts, apps/api/src/index.ts</files>
  <action>
    1. Create a new Hono route `GET /api/events` using `streamSSE`.
    2. Implement a simple event bus or use a shared `EventEmitter` to allow `WorkerService` to broadcast messages to connected SSE clients.
    3. Register the route in `apps/api/src/index.ts`.
    4. Ensure proper cleanup of streams when clients disconnect.
  </action>
  <verify>Use `curl -N http://localhost:3001/api/events` and verify it stays open and receives pings/messages.</verify>
  <done>The API can stream real-time events to clients.</done>
</task>

<task type="auto">
  <name>Task 2: Notify Frontend from Worker</name>
  <files>apps/api/src/services/worker.service.ts</files>
  <action>
    Modify `WorkerService` to emit an event (e.g., 'note_updated') whenever a job completes. This event should include the `noteId` and potentially the new tags (once implemented in Phase 7). For now, it should trigger a signal that the note has changed.
  </action>
  <verify>Observe the `curl` output from Task 1 when a background job completes.</verify>
  <done>Background jobs signal completion to connected SSE clients.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Frontend SSE Listener and UI Polish</name>
  <files>apps/web/src/hooks/useTimeline.ts, apps/web/src/components/NoteCard.tsx</files>
  <action>
    1. In the frontend, create a hook or update `useTimeline` to listen to the SSE endpoint.
    2. When a 'note_updated' event is received, invalidate the TanStack Query cache for that note or the timeline to trigger a refresh.
    3. Update `NoteCard.tsx` (or tag component) to render tags.
    4. Apply a distinct visual style to tags identified as "AI-generated" (e.g., a different background color or a subtle border) as per CONTEXT.md.
  </action>
  <verify>Manually trigger a background job (e.g., by creating a note) and verify the NoteCard updates automatically without a page refresh, showing the "AI tags".</verify>
  <done>The UI reflects background processing results in real-time with visual distinction for AI tags.</done>
</task>

</tasks>

<verification>
- Verify that multiple browser tabs all receive the same SSE update.
- Verify that "AI tags" look different from regular tags (if any exist).
</verification>

<success_criteria>
The end-to-end "Capture -> Background Process -> UI Update" loop is closed and visually verifiable.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-processor/06-03-SUMMARY.md`
</output>
