---
phase: 06-ai-processor
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: [apps/api/src/routes/events.ts, apps/api/src/index.ts, apps/web/src/hooks/useSSE.ts]
autonomous: true

must_haves:
  truths:
    - "Real-time updates are broadcast to connected clients via SSE"
    - "Frontend receives and logs background processing completion events"
    - "SSE connection is cleaned up on client disconnect"
  artifacts:
    - path: "apps/api/src/routes/events.ts"
      provides: "SSE endpoint for real-time updates"
    - path: "apps/web/src/hooks/useSSE.ts"
      provides: "Generic SSE listener hook"
  key_links:
    - from: "apps/web/src/hooks/useSSE.ts"
      to: "/api/events"
      via: "EventSource"
---

<objective>
Implement the real-time update mechanism using Server-Sent Events (SSE) and establish a frontend hook to receive these updates. UI integration (NoteCard updates) is deferred until Phase 4 components are available.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-ai-processor/06-RESEARCH.md
@.planning/phases/06-ai-processor/06-CONTEXT.md
@.planning/phases/06-ai-processor/06-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SSE Endpoint</name>
  <files>apps/api/src/routes/events.ts, apps/api/src/index.ts</files>
  <action>
    1. Create a new Hono route `GET /api/events` using `streamSSE`.
    2. Implement a simple event bus or use a shared `EventEmitter` to allow `WorkerService` to broadcast messages to connected SSE clients.
    3. Register the route in `apps/api/src/index.ts`.
    4. Ensure proper cleanup of streams when clients disconnect.
  </action>
  <verify>Use `curl -N http://localhost:3001/api/events` and verify it stays open and receives pings/messages.</verify>
  <done>The API can stream real-time events to clients.</done>
</task>

<task type="auto">
  <name>Task 2: Notify Frontend from Worker</name>
  <files>apps/api/src/services/worker.service.ts</files>
  <action>
    Modify `WorkerService` to emit an event (e.g., 'note_updated') whenever a job completes. This event should include the `noteId` and potentially the new tags (once implemented in Phase 7). For now, it should trigger a signal that the note has changed.
  </action>
  <verify>Observe the `curl` output from Task 1 when a background job completes.</verify>
  <done>Background jobs signal completion to connected SSE clients.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Frontend SSE Listener (Generic)</name>
  <files>apps/web/src/hooks/useSSE.ts</files>
  <action>
    1. Create a generic `useSSE` hook in `apps/web/src/hooks/useSSE.ts` that connects to `/api/events`.
    2. The hook should listen for 'note_updated' events and log them to the console for now.
    3. Provide an option to invalidate TanStack Query cache if a `queryClient` is available, but keep it optional.
    4. Note: Full UI integration (NoteCard.tsx) is deferred until Phase 4 components are implemented.
  </action>
  <verify>Open browser console, create a note via API/UI, and verify 'note_updated' event is logged when the background job finishes.</verify>
  <done>Frontend can receive real-time signals from the background worker.</done>
</task>

</tasks>

<verification>
- Verify that multiple browser tabs all receive the same SSE update via console logs.
</verification>

<success_criteria>
The API-to-Frontend communication channel via SSE is established and verifiable via logs.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-processor/06-03-SUMMARY.md`
</output>
