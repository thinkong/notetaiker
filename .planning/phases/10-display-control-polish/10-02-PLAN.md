---
phase: 10-display-control-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/App.tsx
  - apps/web/src/components/editor/Editor.tsx
  - apps/web/src/components/layout/StatusIndicator.tsx
autonomous: true
must_haves:
  truths:
    - "User can trigger an immediate save using Ctrl+Enter (Windows/Linux) or Cmd+Enter (macOS) while the editor is focused."
    - "The save action provides immediate visual feedback to the user."
    - "No duplicate save requests are sent if multiple listeners trigger."
  artifacts:
    - path: "apps/web/src/components/editor/Editor.tsx"
      provides: "Editor-level save shortcut handling"
    - path: "apps/web/src/components/layout/StatusIndicator.tsx"
      provides: "Enhanced save feedback visibility"
  key_links:
    - from: "CodeMirror Keymap"
      to: "forceSave function"
      via: "extension prop or callback"
---

<objective>
Standardize the save shortcut behavior and improve visual feedback to ensure users feel confident their work is captured. This satisfies requirement UI-FIX-02.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/App.tsx
@apps/web/src/components/editor/Editor.tsx
@apps/web/src/components/layout/StatusIndicator.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add save shortcut to CodeMirror</name>
  <files>apps/web/src/components/editor/Editor.tsx</files>
  <action>
    1. Update `EditorProps` to accept an `onSave` callback.
    2. In `Editor.tsx`, update the `extensions` memo to include a keymap for "Mod-Enter".
    3. The keymap should call the `onSave` callback and return `true` to indicate the event was handled.
    4. Ensure "Mod-Enter" works for both Ctrl+Enter (Linux/Windows) and Cmd+Enter (macOS).
  </action>
  <verify>
    Check code for the new extension and callback prop.
  </verify>
  <done>
    The editor component supports a native save shortcut that triggers a callback.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire up Editor Save in App.tsx</name>
  <files>apps/web/src/App.tsx</files>
  <action>
    1. Pass `() => forceSave(content)` to the `Editor`'s `onSave` prop.
    2. Refine the existing `useHotkeys` global shortcut to ensure it doesn't conflict or duplicate if the editor also handles it, or rely on the editor-level handler when focused.
  </action>
  <verify>
    1. Open the app.
    2. Focus the editor.
    3. Press Ctrl+Enter (or Cmd+Enter).
    4. Verify the StatusIndicator shows "Saving..." then "Saved".
  </verify>
  <done>
    Global and local save shortcuts are synchronized and functional.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enhance Save Feedback</name>
  <files>apps/web/src/components/layout/StatusIndicator.tsx</files>
  <action>
    Improve the visibility of the "Saved" state in `StatusIndicator.tsx`. Add a brief animation or color change (e.g., flash nord-frost2) when transition to "saved" occurs to make it more obvious that an explicit save was successful.
  </action>
  <verify>
    Observe the StatusIndicator when saving. The feedback should be clear and noticeable.
  </verify>
  <done>
    The user receives clear visual confirmation of a successful save.
  </done>
</task>

</tasks>

<verification>
Check that UI-FIX-02 is satisfied: User can save note using Ctrl+Enter shortcut and receives clear feedback.
</verification>

<success_criteria>
Save interaction is fast, reliable, and provides clear user feedback across all platforms.
</success_criteria>

<output>
After completion, create `.planning/phases/10-display-control-polish/10-02-SUMMARY.md`
</output>
