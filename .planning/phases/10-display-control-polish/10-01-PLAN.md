---
phase: 10-display-control-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/timeline/NoteCard.tsx
  - apps/web/src/types/index.ts
autonomous: true
must_haves:
  truths:
    - "User can toggle a 'Metadata' view on each note card to see technical details."
    - "Metadata view is collapsed by default to maintain a clean timeline."
    - "Metadata is rendered in a styled, monospaced block (Nord theme consistent)."
  artifacts:
    - path: "apps/web/src/components/timeline/NoteCard.tsx"
      provides: "Toggleable metadata section and display logic"
    - path: "apps/web/src/types/index.ts"
      provides: "Updated NoteMetadata interface"
  key_links:
    - from: "NoteCard component"
      to: "note.metadata object"
      via: "useState toggle and conditional rendering"
---

<objective>
Implement a toggleable metadata view in the note cards to hide technical details by default while keeping them accessible. This satisfies requirement UI-FIX-01 by ensuring raw-looking metadata doesn't clutter the primary note body.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/components/timeline/NoteCard.tsx
@apps/web/src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update NoteMetadata type</name>
  <files>apps/web/src/types/index.ts</files>
  <action>
    Refine the `NoteMetadata` interface in `apps/web/src/types/index.ts` to ensure all standard fields (id, createdAt, updatedAt, tags, ai) are explicitly typed as optional or required based on their usage in the app. Add a `[key: string]: unknown` index signature to support custom frontmatter fields.
  </action>
  <verify>
    Check file content for updated interface. Ensure no TypeScript errors in dependent files.
  </verify>
  <done>
    `NoteMetadata` interface reflects the actual structure used by the API and allows for extensible frontmatter.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Metadata Toggle in NoteCard</name>
  <files>apps/web/src/components/timeline/NoteCard.tsx</files>
  <action>
    1. Add `showMetadata` state using `useState(false)` in `NoteCard`.
    2. Add an 'Info' or 'Details' button/icon next to the 'Show more' button or in the header of the card.
    3. Implement a conditional rendering block that shows the `metadata` object (excluding the content itself) when `showMetadata` is true.
    4. Style the metadata block using Nord theme colors (e.g., `bg-nord-snow1 dark:bg-nord-polar2`) and a monospaced font. Use `JSON.stringify(metadata, null, 2)` or a key-value list for the display.
  </action>
  <verify>
    1. Run the web app.
    2. Observe a new toggle for metadata on note cards.
    3. Click the toggle and verify the metadata appears in a styled block.
    4. Verify it is collapsed by default.
  </verify>
  <done>
    NoteCard has a functional, styled, and toggleable metadata display.
  </done>
</task>

</tasks>

<verification>
Check that UI-FIX-01 is satisfied: Note stream view handles metadata intelligently and excludes raw technical data from the main body display unless requested.
</verification>

<success_criteria>
Timeline view remains clean while providing deep access to note metadata on demand.
</success_criteria>

<output>
After completion, create `.planning/phases/10-display-control-polish/10-01-SUMMARY.md`
</output>
