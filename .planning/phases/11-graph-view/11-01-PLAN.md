---
phase: 11-graph-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  [
    "package.json",
    "apps/web/src/App.tsx",
    "apps/web/src/hooks/useGraphData.ts",
    "apps/web/src/components/graph/GraphView.tsx",
  ]
autonomous: true

must_haves:
  truths:
    - "User can navigate to /graph route"
    - "System fetches all notes for the graph"
    - "Graph data is correctly transformed into Note and Tag nodes"
  artifacts:
    - path: "apps/web/src/hooks/useGraphData.ts"
      provides: "Graph data transformation logic"
    - path: "apps/web/src/components/graph/GraphView.tsx"
      provides: "Entry point for the graph feature"
  key_links:
    - from: "apps/web/src/components/graph/GraphView.tsx"
      to: "apps/web/src/hooks/useGraphData.ts"
      via: "hook usage"
---

<objective>
Setup the infrastructure for the Graph View, including dependencies, routing, and the data transformation layer that turns flat notes into a "Tag Hub" graph structure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-graph-view/11-CONTEXT.md
@.planning/phases/11-graph-view/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Dependencies and Routing</name>
  <files>package.json, apps/web/src/App.tsx, apps/web/src/components/graph/GraphView.tsx</files>
  <action>
    1. Install `react-force-graph-2d` and `d3-force` in the workspace (or specifically in apps/web if preferred, but usually workspace root or apps/web package.json).
    2. Create a stub `GraphView` component in `apps/web/src/components/graph/GraphView.tsx`.
    3. Add a new route `/graph` in `apps/web/src/App.tsx` and a link to it in the header (next to Settings).
  </action>
  <verify>
    - `npm list react-force-graph-2d` shows the package installed.
    - Navigating to `/graph` in the browser shows the stub component.
  </verify>
  <done>
    Dependencies are installed and the route is accessible.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement useGraphData Hook</name>
  <files>apps/web/src/hooks/useGraphData.ts</files>
  <action>
    Create a hook that:
    1. Fetches notes from the API (use a large limit to ensure "Global Graph" coverage for now).
    2. Transforms notes into a graph structure:
       - Note Nodes: `{ id, type: 'note', name: note.title, content: note.content }`
       - Tag Nodes: `{ id: 'tag-' + tag, type: 'tag', name: '#' + tag }`
       - Links: From Note ID to Tag Node ID for every tag in the note.
    3. Returns `{ nodes, links, isLoading }`.
  </action>
  <verify>
    - Hook correctly identifies unique tags and creates central nodes for them.
    - Multiple notes sharing a tag all link to the same tag node.
  </verify>
  <done>
    Data transformation logic is complete and handles the "Tag Hub" pattern.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes in apps/web.
- /graph route loads and shows "Graph View" text.
</verification>

<success_criteria>
Graph infrastructure is in place with a working data pipeline from API to graph nodes/links.
</success_criteria>

<output>
After completion, create `.planning/phases/11-graph-view/11-01-SUMMARY.md`
</output>
