---
phase: 11-graph-view
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified: ["apps/web/src/components/graph/ForceGraph.tsx", "apps/web/src/components/graph/GraphView.tsx"]
autonomous: false

must_haves:
  truths:
    - "User can see a force-directed graph of notes and tags"
    - "Tag nodes are visually distinct from note nodes"
    - "Labels are visible on hover or at high zoom levels"
  artifacts:
    - path: "apps/web/src/components/graph/ForceGraph.tsx"
      provides: "Canvas-based graph visualization"
  key_links:
    - from: "apps/web/src/components/graph/GraphView.tsx"
      to: "apps/web/src/components/graph/ForceGraph.tsx"
      via: "component rendering"
---

<objective>
Implement the core interactive force-directed graph using react-force-graph-2d, applying the specific visual style and adaptive labeling defined in the context.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-graph-view/11-CONTEXT.md
@.planning/phases/11-graph-view/11-RESEARCH.md
@.planning/phases/11-graph-view/11-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ForceGraph Component</name>
  <files>apps/web/src/components/graph/ForceGraph.tsx</files>
  <action>
    Create a `ForceGraph` component that wraps `react-force-graph-2d`:
    1. Configure `nodeCanvasObject` to paint nodes:
       - Use different colors/sizes for 'note' vs 'tag' nodes.
       - Implement adaptive labeling: only show text labels when `globalScale > threshold` or node is hovered.
    2. Setup physics parameters in `useEffect` (d3-force charge, link distance, etc.).
    3. Implement hover highlighting: dim other nodes/links when a node is hovered.
  </action>
  <verify>
    - Graph renders with nodes and links.
    - Labels appear when zooming in.
    - Hovering a node highlights its neighborhood.
  </verify>
  <done>
    Graph visualization is functional and follows the visual style decisions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Force-directed graph visualization with Tag Hubs.</what-built>
  <how-to-verify>
    1. Navigate to /graph.
    2. Verify notes connect to tags.
    3. Verify zooming and panning works.
    4. Verify labels appear/disappear based on zoom.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- Nodes and links are visible and interactive.
- No console errors from the physics engine or canvas rendering.
</verification>

<success_criteria>
The graph accurately visualizes the note relationships using the Tag Hub pattern with high performance.
</success_criteria>

<output>
After completion, create `.planning/phases/11-graph-view/11-02-SUMMARY.md`
</output>
