---
phase: 11-graph-view
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  [
    "apps/web/src/components/graph/NoteSidePanel.tsx",
    "apps/web/src/components/graph/GraphView.tsx",
  ]
autonomous: true

must_haves:
  truths:
    - "Clicking a note node opens it in a right-side panel"
    - "The side panel can be closed to return to full-screen graph"
    - "The side panel renders the full markdown content of the note"
  artifacts:
    - path: "apps/web/src/components/graph/NoteSidePanel.tsx"
      provides: "Collapsible side panel for note viewing"
  key_links:
    - from: "apps/web/src/components/graph/GraphView.tsx"
      to: "apps/web/src/components/graph/NoteSidePanel.tsx"
      via: "prop passing and state sync"
---

<objective>
Implement the side panel for viewing note details when a node is clicked in the graph, ensuring the user maintains the graph context while reading.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-graph-view/11-CONTEXT.md
@.planning/phases/11-graph-view/11-RESEARCH.md
@.planning/phases/11-graph-view/11-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NoteSidePanel Component</name>
  <files>apps/web/src/components/graph/NoteSidePanel.tsx</files>
  <action>
    1. Create a `NoteSidePanel` component that takes a `Note` object and an `onClose` callback.
    2. Style it as a fixed-position right sidebar (similar to the left sidebar but for the graph view).
    3. Use the existing `Markdown` component to render the note content.
    4. Include note metadata (tags, creation date) in the panel header/footer.
  </action>
  <verify>
    - Component renders correctly when provided with a note.
    - Close button triggers the callback.
  </verify>
  <done>
    Side panel component is ready and follows the project's visual style.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Node Click Handling</name>
  <files>apps/web/src/components/graph/GraphView.tsx</files>
  <action>
    1. Update `GraphView` to manage `selectedNoteId` state.
    2. Pass an `onNodeClick` handler to `ForceGraph` (from Plan 02).
    3. When a note node is clicked, set the `selectedNoteId`.
    4. Render `NoteSidePanel` conditionally when a note is selected.
    5. Ensure the graph container resizes or shifts to accommodate the panel if necessary (or overlay it as per decision).
  </action>
  <verify>
    - Clicking a note node opens the panel.
    - Clicking the close button in the panel clears the selection and hides the panel.
  </verify>
  <done>
    Graph-to-Detail navigation flow is implemented.
  </done>
</task>

</tasks>

<verification>
- Clicking multiple notes sequentially updates the side panel correctly.
- Side panel is responsive and doesn't break the graph layout.
</verification>

<success_criteria>
Users can explore the graph and drill down into individual notes without losing their visual orientation.
</success_criteria>

<output>
After completion, create `.planning/phases/11-graph-view/11-03-SUMMARY.md`
</output>
