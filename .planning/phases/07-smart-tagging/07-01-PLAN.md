---
phase: 07-smart-tagging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["apps/api/package.json", "apps/api/src/services/ai.service.ts", "apps/api/src/services/ai.service.test.ts"]
autonomous: true
user_setup:
  - service: AI Providers
    why: "Required for tag generation"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard"
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Dashboard"

must_haves:
  truths:
    - "AIService can generate 3-5 tags for a given text"
    - "AIService selects correct provider based on available keys"
  artifacts:
    - path: "apps/api/src/services/ai.service.ts"
      provides: "AI interaction logic"
  key_links:
    - from: "apps/api/src/services/ai.service.ts"
      to: "Vercel AI SDK"
      via: "generateObject"
---

<objective>
Implement the core AI service responsible for generating structured tags from note content using the Vercel AI SDK.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-smart-tagging/07-RESEARCH.md
@apps/api/src/services/secrets.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install AI SDK dependencies</name>
  <files>apps/api/package.json</files>
  <action>
    Install Vercel AI SDK and providers:
    - `npm install ai @ai-sdk/openai @ai-sdk/anthropic @ai-sdk/google` in `apps/api`.
  </action>
  <verify>Check package.json for new dependencies.</verify>
  <done>Dependencies are installed and ready to use.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AIService</name>
  <files>apps/api/src/services/ai.service.ts</files>
  <action>
    Create `AIService` class that:
    1. Accepts `SecretsService` in constructor.
    2. Implements `generateTags(content: string): Promise<string[]>` method.
    3. Uses `generateObject` from `ai` SDK with a Zod schema for `{ tags: string[] }`.
    4. Dynamically selects provider (OpenAI, Anthropic, or Gemini) based on which API keys are present in secrets.
    5. Uses a prompt like: "Generate 3-5 relevant, Title Case tags for this note content. Return only the tags."
  </action>
  <verify>Run a small script or unit test to call generateTags with sample text.</verify>
  <done>AIService returns a valid array of strings when provided with content.</done>
</task>

<task type="tdd">
  <name>Task 3: Test AIService Provider Selection</name>
  <files>apps/api/src/services/ai.service.test.ts</files>
  <action>
    Write unit tests for `AIService` to ensure it:
    - Correctly initializes the right provider based on mock secrets.
    - Handles missing keys gracefully (throws or returns empty).
  </action>
  <verify>npm test apps/api/src/services/ai.service.test.ts</verify>
  <done>Tests pass for provider selection logic.</done>
</task>

</tasks>

<verification>
Verify AIService can be instantiated and returns structured tags using at least one provider (mocked or real).
</verification>

<success_criteria>
AIService is implemented, tested, and able to produce tags from text using the Vercel AI SDK.
</success_criteria>

<output>
After completion, create `.planning/phases/07-smart-tagging/07-01-SUMMARY.md`
</output>
