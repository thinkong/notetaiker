---
phase: 07-smart-tagging
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: ["apps/web/src/components/NoteCard.tsx", "apps/web/src/types/index.ts"]
autonomous: true

must_haves:
  truths:
    - "User can see AI-generated tags on note cards in the timeline"
    - "Tags update in real-time when the background worker finishes"
  artifacts:
    - path: "apps/web/src/components/NoteCard.tsx"
      provides: "Tag visualization"
---

<objective>
Display the AI-generated tags in the frontend UI to complete the "Smart Tagging" experience.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@apps/web/src/components/NoteCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Frontend Types</name>
  <files>apps/web/src/types/index.ts</files>
  <action>
    Update the `Note` and `NoteMetadata` types in the frontend to include `tags` (as a string array).
  </action>
  <verify>Compiler check in frontend.</verify>
  <done>Frontend types match backend response.</done>
</task>

<task type="auto">
  <name>Task 2: Display Tags in NoteCard</name>
  <files>apps/web/src/components/NoteCard.tsx</files>
  <action>
    - Modify `NoteCard` to render a list of small, styled tag badges below the note preview.
    - Use subtle Tailwind classes (e.g., `bg-gray-100 text-gray-600 px-2 py-0.5 rounded-full text-xs`).
  </action>
  <verify>Open browser, create a note, wait for worker completion, and observe tags appearing on the card.</verify>
  <done>Tags are visible and correctly styled in the UI.</done>
</task>

</tasks>

<verification>
Manual E2E:
1. Start app.
2. Capture a note about "React and TypeScript".
3. Wait for SSE event / update.
4. Confirm "React", "TypeScript" (or similar) tags appear on the card.
</verification>

<success_criteria>
Smart tagging is fully integrated and visible to the user, providing immediate organizational value.
</success_criteria>

<output>
After completion, create `.planning/phases/07-smart-tagging/07-03-SUMMARY.md`
</output>
