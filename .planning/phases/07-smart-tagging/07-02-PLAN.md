---
phase: 07-smart-tagging
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  [
    "apps/api/src/services/worker.service.ts",
    "apps/api/src/services/storage.service.ts",
    "apps/api/src/lib/markdown.ts",
  ]
autonomous: true

must_haves:
  truths:
    - "Background worker calls AI service to get tags"
    - "Tags are merged with existing tags in Markdown frontmatter"
    - "Notes with 'ai: false' are skipped by the worker"
  artifacts:
    - path: "apps/api/src/lib/markdown.ts"
      provides: "Tag merging logic"
  key_links:
    - from: "WorkerService"
      to: "AIService"
      via: "generateTags call"
---

<objective>
Integrate the AI tagging service into the background worker and update the storage engine to merge and persist these tags into note files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@apps/api/src/services/worker.service.ts
@apps/api/src/services/storage.service.ts
@apps/api/src/lib/markdown.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update NoteFrontmatterSchema and Merge Logic</name>
  <files>apps/api/src/lib/markdown.ts</files>
  <action>
    - Update `NoteFrontmatterSchema` in `apps/api/src/lib/markdown.ts` to include an optional `tags: z.array(z.string())` field (or `z.string()` if using comma-separated).
    - Implement a helper function `mergeTags(existing: string[] | string | undefined, newTags: string[]): string[]` that ensures uniqueness and Title Case.
  </action>
  <verify>Check schema definition and run unit tests for merge logic.</verify>
  <done>Schema supports tags and merge logic is robust.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate AIService into WorkerService</name>
  <files>apps/api/src/services/worker.service.ts</files>
  <action>
    - Update `WorkerService` constructor to accept `AIService` and `StorageService`.
    - In `executeJob`:
      1. Fetch note content via `StorageService.getNote(noteId)`.
      2. Check if `metadata.ai === false`. If so, skip processing and mark as completed.
      3. Call `aiService.generateTags(content)`.
      4. Merge generated tags with existing `metadata.tags`.
      5. Call `storageService.saveNote(content, updatedMetadata)` to persist tags.
  </action>
  <verify>Create a note, wait for worker, and check if the .md file now has tags in its frontmatter.</verify>
  <done>Worker successfully tags notes and saves them to disk.</done>
</task>

<task type="auto">
  <name>Task 3: Update StorageService for metadata updates</name>
  <files>apps/api/src/services/storage.service.ts</files>
  <action>
    Ensure `saveNote` correctly handles updating existing notes when an ID is provided, preserving other metadata fields while updating `updatedAt` and merging `tags`.
  </action>
  <verify>Verify that calling saveNote with an existing ID updates the file instead of creating a new one.</verify>
  <done>StorageService handles updates correctly.</done>
</task>

</tasks>

<verification>
E2E check: Create a note -> Worker picks it up -> AI generates tags -> File is updated with tags in frontmatter -> Event broadcasted.
</verification>

<success_criteria>
Notes are automatically enriched with AI-generated tags in the background, respecting opt-out and preserving manual tags.
</success_criteria>

<output>
After completion, create `.planning/phases/07-smart-tagging/07-02-SUMMARY.md`
</output>
