---
phase: 13-manual-tag-control
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - apps/web/src/components/timeline/NoteCard.tsx
  - apps/web/src/components/sidebar/SidebarNoteCard.tsx
  - apps/web/src/components/graph/NoteSidePanel.tsx
autonomous: true
must_haves:
  truths:
    - "Manual tags are visually distinct from AI-generated tags (e.g. Blue vs Purple)"
    - "AI tags have a 'dismiss' button that removes them from the view"
    - "Dismissing an AI tag prevents it from reappearing after the next background worker run"
  artifacts:
    - path: "apps/web/src/components/common/Tag.tsx"
      provides: "Unified Tag component with variant support and dismissal"
  key_links:
    - from: "Tag component"
      to: "API updateNote"
      via: "onDismiss callback"
---

<objective>
Update the UI to distinguish between manual and AI tags, and provide a way for users to reject AI suggestions.

Purpose: Give users full control over the organizational metadata and clear visibility into what the AI has suggested.
Output: A new shared Tag component and updated note views.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/src/components/timeline/NoteCard.tsx
@apps/web/src/components/sidebar/SidebarNoteCard.tsx
@apps/web/src/components/graph/NoteSidePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Unified Tag Component</name>
  <files>apps/web/src/components/common/Tag.tsx</files>
  <action>
    - Create a `Tag` component that accepts:
      - `label`: string
      - `variant`: 'manual' | 'ai'
      - `onDismiss?`: () => void
    - Styling (using Tailwind/Nord):
      - 'manual': Blue (Nord Frost 2/3)
      - 'ai': Purple (Nord Aurora 4 - #b48ead)
    - If `onDismiss` is provided, show a small 'x' button for AI tags.
  </action>
  <verify>
    Check the component renders correctly with both variants.
  </verify>
  <done>
    A reusable Tag component exists with distinct visual styles for manual and AI tags.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Note Views and Implement Dismissal</name>
  <files>
    - apps/web/src/components/timeline/NoteCard.tsx
    - apps/web/src/components/sidebar/SidebarNoteCard.tsx
    - apps/web/src/components/graph/NoteSidePanel.tsx
  </files>
  <action>
    - Replace inline tag mapping with the new `Tag` component.
    - Render `metadata.tags` as 'manual' and `metadata.ai_tags` as 'ai'.
    - Implement `handleDismissTag(tag)` in `NoteCard`:
      - Call the API to update the note.
      - Add the tag to `ignored_tags`.
      - Remove the tag from `ai_tags`.
    - Ensure `SidebarNoteCard` and `NoteSidePanel` also reflect the dual-source tags.
  </action>
  <verify>
    1. Open a note with both manual and AI tags.
    2. Confirm they look different.
    3. Click 'x' on an AI tag.
    4. Refresh or wait for save -> confirm it is gone and doesn't come back.
  </verify>
  <done>
    All note views correctly display and allow interaction with manual and AI tags.
  </done>
</task>

</tasks>

<verification>
1. Manual tags (from body hashtags or frontmatter) are Blue.
2. AI tags (from worker) are Purple and have a dismiss button.
3. Dismissing a tag adds it to `ignored_tags` in the note frontmatter.
</verification>

<success_criteria>
Users have clear visual distinction between their tags and AI tags, with the ability to curate AI suggestions.
</success_criteria>

<output>
After completion, create `.planning/phases/13-manual-tag-control/13-03-SUMMARY.md`
</output>
