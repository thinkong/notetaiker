---
phase: 13-manual-tag-control
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - apps/web/src/components/editor/Editor.tsx
  - apps/web/src/components/editor/extensions/hashtags.ts
  - apps/api/src/routes/notes.ts
autonomous: true
must_haves:
  truths:
    - "Typing #hashtag in the editor highlights it visually"
    - "Typing # triggers a dropdown suggesting existing tags"
    - "Saving a note extracts body hashtags into the tags frontmatter field"
  artifacts:
    - path: "apps/web/src/components/editor/extensions/hashtags.ts"
      provides: "CodeMirror extension for highlighting and autocomplete"
    - path: "apps/api/src/routes/notes.ts"
      provides: "Server-side hashtag promotion on save"
  key_links:
    - from: "Editor.tsx"
      to: "hashtags.ts"
      via: "extension registration"
    - from: "POST /api/notes"
      to: "extractHashtags"
      via: "metadata update before save"
---

<objective>
Implement rich editor support for hashtags and ensure they are automatically promoted to note metadata on save.

Purpose: Provide a frictionless "type-to-tag" experience that feels native to the writing flow.
Output: CodeMirror hashtag highlighter, tag autocompletion, and server-side tag extraction.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/src/components/editor/Editor.tsx
@apps/api/src/routes/notes.ts
@apps/api/src/lib/markdown.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Hashtag Extension</name>
  <files>apps/web/src/components/editor/extensions/hashtags.ts</files>
  <action>
    - Create a new extension file using `@codemirror/view` and `@codemirror/autocomplete`.
    - Implement `hashtagHighlighter` using `MatchDecorator` for `#\w+` patterns.
    - Implement `hashtagAutocomplete` as a `CompletionSource`.
    - Autocomplete should fetch existing tags (manual and AI) from the notes list or a dedicated tags endpoint if available (otherwise just from the current notes cache).
  </action>
  <verify>
    Verify the extension exports the necessary CodeMirror components.
  </verify>
  <done>
    Hashtag logic is encapsulated in a reusable extension file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Extension and Promote Tags</name>
  <files>
    - apps/web/src/components/editor/Editor.tsx
    - apps/api/src/routes/notes.ts
  </files>
  <action>
    - Register the hashtag extension in `Editor.tsx`.
    - Update `apps/api/src/routes/notes.ts` in the `POST /` (save) handler:
      - Call `extractHashtags(content)` on the incoming note content.
      - Merge the extracted tags into the `metadata.tags` field.
      - Ensure this promotion happens *before* saving to disk.
  </action>
  <verify>
    1. Type `#work` in the editor -> check it is highlighted.
    2. Save note -> check file frontmatter contains `tags: [Work]`.
    3. Verify # is suggested in autocomplete.
  </verify>
  <done>
    Editor highlights hashtags and server-side persistence automatically indexes them.
  </done>
</task>

</tasks>

<verification>
1. Visual highlighting of hashtags in the editor.
2. Functional autocomplete for tags.
3. Automatic sync between body hashtags and frontmatter tags.
</verification>

<success_criteria>
Users can tag notes simply by writing `#tag` in the body, with immediate visual feedback and metadata persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/13-manual-tag-control/13-02-SUMMARY.md`
</output>
