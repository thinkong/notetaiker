---
phase: 13-manual-tag-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/lib/markdown.ts
  - apps/api/src/services/worker.service.ts
autonomous: true
must_haves:
  truths:
    - "Note frontmatter supports ai_tags and ignored_tags fields"
    - "AI worker stores suggestions in ai_tags, leaving tags untouched"
    - "Tags in ignored_tags are never re-added by the AI worker"
  artifacts:
    - path: "apps/api/src/lib/markdown.ts"
      provides: "Updated schema and hashtag extraction utility"
    - path: "apps/api/src/services/worker.service.ts"
      provides: "Refactored AI tag merge logic"
  key_links:
    - from: "WorkerService"
      to: "ai_tags"
      via: "saveNote"
      pattern: "ai_tags:.*"
---

<objective>
Update the core data model and background worker to support the distinction between manual and AI-generated tags.

Purpose: Prevent AI from overwriting user intent and allow users to reject AI suggestions permanently.
Output: Updated Zod schema, a hashtag extraction utility, and a refactored WorkerService.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/api/src/lib/markdown.ts
@apps/api/src/services/worker.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Schema and Extraction Utility</name>
  <files>apps/api/src/lib/markdown.ts</files>
  <action>
    - Add `ai_tags: z.array(z.string()).optional()` and `ignored_tags: z.array(z.string()).optional()` to `NoteFrontmatterSchema`.
    - Implement `extractHashtags(content: string): string[]` utility.
    - Regexp should match `#(\w+)` and normalize to Title Case (consistent with existing `mergeTags`).
    - Deduplicate results.
  </action>
  <verify>
    Run `pnpm test` in `apps/api` (ensure existing tests pass) and add a small test case for `extractHashtags`.
  </verify>
  <done>
    `NoteFrontmatter` includes new fields and `extractHashtags` correctly identifies tags in body text.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor WorkerService Tag Logic</name>
  <files>apps/api/src/services/worker.service.ts</files>
  <action>
    - Modify `executeJob` to use `ai_tags` for storage.
    - Change tag selection logic:
      1. Get `generatedTags` from AI.
      2. Filter out tags that are already in `note.metadata.tags` (manual).
      3. Filter out tags that are in `note.metadata.ignored_tags` (dismissed).
      4. Store the remainder in `ai_tags`.
    - Ensure `note.metadata.tags` is NEVER modified by the worker.
  </action>
  <verify>
    Check log output when worker runs: "Worker: Updated ai_tags for note...".
    Verify `tags` field in frontmatter remains unchanged by AI.
  </verify>
  <done>
    AI worker strictly manages `ai_tags` and respects user dismissals/manual tags.
  </done>
</task>

</tasks>

<verification>
1. Frontmatter schema supports `ai_tags` and `ignored_tags`.
2. WorkerService no longer touches the `tags` array.
3. AI suggestions are saved to `ai_tags`.
</verification>

<success_criteria>
AI-generated tags are isolated from manual tags in the filesystem frontmatter.
</success_criteria>

<output>
After completion, create `.planning/phases/13-manual-tag-control/13-01-SUMMARY.md`
</output>
