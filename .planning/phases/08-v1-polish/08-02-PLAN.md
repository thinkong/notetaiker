---
phase: 08-v1-polish
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: ["apps/web/package.json", "apps/web/src/App.tsx", "apps/web/src/components/search/SearchPalette.tsx"]
autonomous: true

must_haves:
  truths:
    - "User can open a search/command palette with Cmd+K (or Ctrl+K)"
    - "User can search through notes by title/content in the palette"
    - "User can save a note using Cmd+Enter (or Ctrl+Enter) while focused in the editor"
  artifacts:
    - path: "apps/web/src/components/search/SearchPalette.tsx"
      provides: "Fuzzy search interface for notes"
  key_links:
    - from: "apps/web/src/App.tsx"
      to: "apps/web/src/components/search/SearchPalette.tsx"
      via: "Hotkeys trigger"
---

<objective>
Implement keyboard-driven navigation and search to provide a power-user experience. This includes a "Cmd+K" command palette for searching notes and global shortcuts for common actions like saving.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-v1-polish/08-RESEARCH.md
@apps/web/src/App.tsx
@apps/web/src/components/editor/Editor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Keyboard & Search Dependencies</name>
  <files>apps/web/package.json</files>
  <action>
    Install `cmdk` for the command palette and `react-hotkeys-hook` for keyboard shortcuts.
    Also install `lucide-react` if not already present for icons.
  </action>
  <verify>
    Check `package.json` for new dependencies.
  </verify>
  <done>
    Dependencies are available for implementation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement SearchPalette Component</name>
  <files>apps/web/src/components/search/SearchPalette.tsx</files>
  <action>
    Create a `SearchPalette` component using `cmdk`.
    It should:
    - Fetch the list of notes (use TanStack Query).
    - Provide a fuzzy search interface.
    - Allow navigating to a note (scroll to it or select it).
    - Style it with Nord theme colors and a backdrop blur.
  </action>
  <verify>
    Manually trigger the component (temp button) and verify search/rendering works.
  </verify>
  <done>
    SearchPalette is functional and styled.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate Shortcuts in App</name>
  <files>apps/web/src/App.tsx</files>
  <action>
    Use `useHotkeys` from `react-hotkeys-hook` to:
    - Bind `Cmd+K` (and `Ctrl+K`) to toggle the `SearchPalette`.
    - Bind `Cmd+Enter` (and `Ctrl+Enter`) to trigger a save action (ensure it works when the editor is focused).
    - Ensure `Esc` closes the palette.
  </action>
  <verify>
    Press `Cmd+K` to open the palette.
    Type in a note content, press `Cmd+Enter` and verify the status indicator shows saving.
  </verify>
  <done>
    App is keyboard-controllable.
  </done>
</task>

</tasks>

<verification>
Verify no shortcut collisions with browser defaults.
Ensure shortcuts are accessible and responsive.
</verification>

<success_criteria>
- Cmd+K opens search palette in < 50ms.
- Cmd+Enter triggers save successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/08-v1-polish/08-02-SUMMARY.md`
</output>
