---
phase: 17-smart-guard-integration-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["apps/web/src/App.tsx", "apps/web/src/hooks/useNavigationGuard.ts"]
autonomous: true
must_haves:
  truths:
    - "Background saves update the baseline for dirty checks"
    - "Navigation guard automatically proceeds when content becomes clean during a block"
  artifacts:
    - path: "apps/web/src/App.tsx"
      provides: "wiring for debounced save"
    - path: "apps/web/src/hooks/useNavigationGuard.ts"
      provides: "auto-proceed watchdog logic"
  key_links:
    - from: "handleContentChange in App.tsx"
      to: "save in useDebouncedSave"
      via: "function call"
---

<objective>
Restore integrity of the Smart Dirty Check by wiring background saves to baseline updates and adding auto-proceed logic to the navigation guard.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/App.tsx
@apps/web/src/hooks/useNavigationGuard.ts
@apps/web/src/hooks/useDebouncedSave.ts
</context>

<tasks>

<task type="auto">
  <name>Wire debounced save in App.tsx</name>
  <files>apps/web/src/App.tsx</files>
  <action>
    Update the `handleContentChange` function in `apps/web/src/App.tsx` to call the `save` function from the `useDebouncedSave` hook. This ensures that typing triggers a background save cycle.
  </action>
  <verify>
    Check that typing in the editor triggers the "Saving..." status indicator after the debounce delay.
  </verify>
  <done>
    `handleContentChange` calls `save(newContent)`.
  </done>
</task>

<task type="auto">
  <name>Add auto-proceed to useNavigationGuard</name>
  <files>apps/web/src/hooks/useNavigationGuard.ts</files>
  <action>
    Add a `useEffect` watchdog to `apps/web/src/hooks/useNavigationGuard.ts` that monitors `isDirty` and the blocker state. If `!isDirty` and the blocker is in the "blocked" state, it should call `blocker.proceed()`. This handles the case where a background save finishes while the confirmation dialog is open.
  </action>
  <verify>
    Trigger a navigation block, wait for background save to finish, and confirm navigation proceeds automatically.
  </verify>
  <done>
    Watchdog effect implemented in `useNavigationGuard`.
  </done>
</task>

</tasks>

<verification>
1. Open the editor and type some content.
2. Wait for background save to finish (Status: Saved).
3. Try to navigate away or click "New Note". It should succeed immediately.
4. Type content and immediately trigger a navigation block.
5. Wait for the save to complete while the dialog is open. The dialog should disappear and the navigation should proceed.
</verification>

<success_criteria>
Smart Dirty Check is fully integrated with background saves, ensuring zero-friction navigation when data is safe.
</success_criteria>
