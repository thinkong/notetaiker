---
phase: 17-smart-guard-integration-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  ["apps/web/src/App.tsx", "apps/web/src/hooks/useNavigationGuard.ts"]
autonomous: true
must_haves:
  truths:
    - "Background saves update the baseline for dirty checks"
    - "Navigation guard automatically proceeds when content becomes clean during a block"
  artifacts:
    - path: "apps/web/src/App.tsx"
      provides: "wiring for debounced save"
    - path: "apps/web/src/hooks/useNavigationGuard.ts"
      provides: "auto-proceed watchdog logic"
  key_links:
    - from: "handleContentChange in App.tsx"
      to: "save in useDebouncedSave"
      via: "function call"
---

<objective>
Restore integrity of the Smart Dirty Check by wiring background saves to baseline updates and adding auto-proceed logic to the navigation guard.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/App.tsx
@apps/web/src/hooks/useNavigationGuard.ts
@apps/web/src/hooks/useDebouncedSave.ts
</context>

<tasks>

<task type="auto">
  <name>Verify and fix debounced save wiring in App.tsx</name>
  <files>apps/web/src/App.tsx</files>
  <action>
    Verify that `handleContentChange` in `apps/web/src/App.tsx` correctly calls the `save` function from `useDebouncedSave`. While the current codebase appears to have this wiring, ensure it is correctly integrated with the `originalContent` baseline updates to prevent "dirty" states after a successful background save.
  </action>
  <verify>
    Check that typing in the editor triggers the "Saving..." status indicator after the debounce delay and that once "Saved" appears, navigation is no longer blocked.
  </verify>
  <done>
    `handleContentChange` calls `save(newContent)` and background saves successfully update the baseline.
  </done>
</task>

<task type="auto">
  <name>Fix auto-proceed watchdog in useNavigationGuard</name>
  <files>apps/web/src/hooks/useNavigationGuard.ts</files>
  <action>
    Update the `useEffect` watchdog in `apps/web/src/hooks/useNavigationGuard.ts`. Currently, it may only call `blocker.proceed()`, which ignores manual actions stored in `pendingAction`. It should be updated to call the internal `proceed()` function which handles both the router blocker and manual actions whenever `isDirty` becomes false while the guard is in a blocked state.
  </action>
  <verify>
    1. Trigger a manual action (e.g., click "New Note") while the editor is dirty.
    2. Wait for background save to finish while the confirmation dialog is open.
    3. Confirm the dialog closes and the manual action (clearing the editor) proceeds automatically.
  </verify>
  <done>
    Watchdog effect correctly calls the internal `proceed()` function to handle all blocked actions when content becomes clean.
  </done>
</task>

</tasks>

<verification>
1. Open the editor and type some content.
2. Wait for background save to finish (Status: Saved).
3. Try to navigate away or click "New Note". It should succeed immediately.
4. Type content and immediately trigger a navigation block (either via link or "New Note").
5. Wait for the save to complete while the dialog is open. The dialog should disappear and the navigation/action should proceed.
</verification>

<success_criteria>
Smart Dirty Check is fully integrated with background saves, ensuring zero-friction navigation when data is safe, including handling of manual internal actions.
</success_criteria>
