---
phase: 05-ai-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "apps/api/src/services/secrets.service.ts",
  "apps/api/src/routes/settings.ts",
  "apps/api/src/index.ts",
  "packages/env/index.ts"
]
autonomous: true

must_haves:
  truths:
    - "Secrets are saved to .notetaiker/secrets.json with 0600 permissions"
    - ".notetaiker is automatically added to .gitignore"
    - "API exposes GET /settings and POST /settings endpoints"
  artifacts:
    - path: "apps/api/src/services/secrets.service.ts"
      provides: "Secure secrets persistence"
    - path: "apps/api/src/routes/settings.ts"
      provides: "Settings API routes"
  key_links:
    - from: "apps/api/src/routes/settings.ts"
      to: "apps/api/src/services/secrets.service.ts"
      via: "service call"
---

<objective>
Implement the backend foundation for secrets management. This includes the storage service that handles atomic writes with restricted permissions and the API routes to manage configuration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-ai-configuration/05-RESEARCH.md
@apps/api/src/index.ts
@packages/env/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Secrets Schema and Install Dependencies</name>
  <files>packages/env/index.ts, apps/api/package.json</files>
  <action>
    1. Install `write-file-atomic` in `apps/api`.
    2. Add `SecretsSchema` to `packages/env/index.ts` to define the structure of `secrets.json` (openai, anthropic, gemini with apiKey and optional baseUrl).
    3. Update `envSchema` if any new static environment variables are needed for this phase.
  </action>
  <verify>
    - `pnpm -F @notetaiker/api list write-file-atomic` shows the package.
    - Zod schema for secrets is exported from `@notetaiker/env`.
  </verify>
  <done>Dependencies installed and schemas defined.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SecretsService</name>
  <files>apps/api/src/services/secrets.service.ts</files>
  <action>
    Create `SecretsService` to:
    1. Resolve path to `.notetaiker/secrets.json` relative to workspace root.
    2. Implement `getSecrets()`: Read and parse JSON, return defaults if missing.
    3. Implement `saveSecrets(data)`: Use `write-file-atomic` with `mode: 0o600`.
    4. Implement `.gitignore` management: Ensure `.notetaiker` is present in the workspace root's `.gitignore`.
  </action>
  <verify>
    - Create a small test script or unit test to verify file creation with 0600 permissions.
    - Verify `.gitignore` is updated.
  </verify>
  <done>SecretsService correctly handles file I/O and security.</done>
</task>

<task type="auto">
  <name>Task 3: Create Settings Routes</name>
  <files>apps/api/src/routes/settings.ts, apps/api/src/index.ts</files>
  <action>
    1. Create `apps/api/src/routes/settings.ts` with:
       - `GET /`: Returns current secrets (masking keys for safety, or returning them if needed by frontend - research suggests masking in UI but API returns full for editing). Actually, return full keys so user can see what they have, UI will mask.
       - `POST /`: Validates input against Zod schema and saves via `SecretsService`.
    2. Mount the routes in `apps/api/src/index.ts` under `/settings`.
  </action>
  <verify>
    - `curl http://localhost:3001/settings` returns JSON.
    - `curl -X POST -H "Content-Type: application/json" -d '{"openai": {"apiKey": "test"}}' http://localhost:3001/settings` persists the data.
  </verify>
  <done>API endpoints for settings are functional.</done>
</task>

</tasks>

<verification>
- [ ] Secrets file exists with 0600 permissions.
- [ ] .gitignore contains .notetaiker.
- [ ] GET/POST /settings work via curl.
</verification>

<success_criteria>
Backend storage for secrets is implemented and exposed via type-safe API routes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-configuration/05-01-SUMMARY.md`
</output>
