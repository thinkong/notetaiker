---
phase: 05-ai-configuration
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified: [
  "apps/web/src/components/settings/SettingsPage.tsx"
]
autonomous: false

must_haves:
  truths:
    - "Existing settings are loaded from the backend on page load"
    - "Clicking 'Save' persists settings to the backend"
    - "Clicking 'Test' validates the specific provider's key"
    - "UI shows success/error feedback for both saving and testing"
  artifacts:
    - path: "apps/web/src/components/settings/SettingsPage.tsx"
      provides: "E2E settings integration"
  key_links:
    - from: "apps/web/src/components/settings/SettingsPage.tsx"
      to: "GET /settings"
      via: "TanStack Query fetch"
    - from: "apps/web/src/components/settings/SettingsPage.tsx"
      to: "POST /settings"
      via: "Mutation"
---

<objective>
Connect the Settings UI to the backend API. This includes loading existing configuration, persisting changes, and providing real-time feedback for credential validation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-ai-configuration/05-01-PLAN.md
@.planning/phases/05-ai-configuration/05-02-PLAN.md
@.planning/phases/05-ai-configuration/05-03-PLAN.md
@apps/web/src/components/settings/SettingsPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load and Save Settings</name>
  <files>apps/web/src/components/settings/SettingsPage.tsx</files>
  <action>
    1. Use TanStack Query to fetch current settings from `GET /settings` and populate the form.
    2. Implement a mutation for `POST /settings` that triggers when the "Save" button is clicked.
    3. Add loading states and success/error notifications (using a toast library or simple status text).
  </action>
  <verify>
    - Refreshing the page keeps the saved values in the form.
    - Saving with new values updates the `.notetaiker/secrets.json` file on disk.
  </verify>
  <done>Settings are persisted between the UI and backend.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Test Connection</name>
  <files>apps/web/src/components/settings/SettingsPage.tsx, apps/web/src/components/settings/ProviderSection.tsx</files>
  <action>
    1. Add a "Test Connection" button to each provider section.
    2. Implement a mutation for `POST /settings/validate` that sends the current form values for that provider.
    3. Display a clear visual indicator of the result (e.g., green checkmark for success, red error message for failure).
  </action>
  <verify>
    - Testing an invalid key shows a descriptive error message from the provider.
    - Testing a valid key (if available) or a mocked successful response shows success.
  </verify>
  <done>Users can verify their credentials directly from the settings page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>End-to-end AI configuration flow</what-built>
  <how-to-verify>
    1. Open the Settings page.
    2. Enter a fake key for OpenAI and click "Test". Verify it fails with a real error from OpenAI.
    3. Enter a valid key (if you have one) or observe the failure message specificity.
    4. Click "Save" and refresh the page. Verify the key (masked) is still there.
    5. Verify `.notetaiker/secrets.json` exists in the project root and is NOT tracked by git.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- [ ] Settings load on mount.
- [ ] Save updates local file.
- [ ] Test button provides live feedback.
</verification>

<success_criteria>
The user can securely configure and validate their AI service credentials through the web interface.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-configuration/05-04-SUMMARY.md`
</output>
