---
phase: 05-ai-configuration
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [
  "apps/web/src/App.tsx",
  "apps/web/src/components/settings/SettingsPage.tsx",
  "apps/web/src/components/settings/ProviderSection.tsx"
]
autonomous: false

must_haves:
  truths:
    - "User can navigate to a /settings page"
    - "UI shows separate sections for OpenAI, Anthropic, and Gemini"
    - "API keys are masked by default with a show/hide toggle"
  artifacts:
    - path: "apps/web/src/components/settings/SettingsPage.tsx"
      provides: "Main settings interface"
    - path: "apps/web/src/components/settings/ProviderSection.tsx"
      provides: "Reusable provider config UI"
  key_links:
    - from: "apps/web/src/App.tsx"
      to: "SettingsPage"
      via: "Routing"
---

<objective>
Create the Settings UI shell and the provider-specific configuration forms. This plan focuses on the layout, form state management, and visual security (key masking).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-ai-configuration/05-CONTEXT.md
@apps/web/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Settings Page and Routing</name>
  <files>apps/web/src/App.tsx, apps/web/src/components/settings/SettingsPage.tsx</files>
  <action>
    1. Create `apps/web/src/components/settings/SettingsPage.tsx` as a basic shell.
    2. Update `apps/web/src/App.tsx` to include a route for `/settings`.
    3. Add a "Settings" link/icon to the main layout (e.g., in a header or floating button) to allow navigation.
  </action>
  <verify>
    - Navigating to `/settings` renders the SettingsPage component.
  </verify>
  <done>Settings route is accessible.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Provider Form Sections</name>
  <files>apps/web/src/components/settings/SettingsPage.tsx, apps/web/src/components/settings/ProviderSection.tsx</files>
  <action>
    1. Install `react-hook-form` and `lucide-react` in `apps/web`.
    2. Create `ProviderSection.tsx` to handle the UI for a single AI provider (Key input, Base URL input, Show/Hide toggle).
    3. Implement the main form in `SettingsPage.tsx` using `react-hook-form`, incorporating sections for OpenAI, Anthropic, and Gemini.
    4. Use Tailwind CSS for a clean, minimalist layout consistent with the Nord theme.
  </action>
  <verify>
    - UI shows three distinct sections.
    - Password toggles work for API key inputs.
    - Form state is managed (can type into fields).
  </verify>
  <done>Settings UI is built with functional form fields.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Settings UI layout and navigation</what-built>
  <how-to-verify>
    1. Start the web app.
    2. Click the Settings link/icon.
    3. Verify you are on the /settings page.
    4. Check that OpenAI, Anthropic, and Gemini sections are visible.
    5. Test the show/hide toggle on the API key fields.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- [ ] /settings route works.
- [ ] Form displays fields for all 3 providers.
- [ ] Masking toggle works.
</verification>

<success_criteria>
The Settings UI is implemented and visually consistent with the project's design goals.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-configuration/05-03-SUMMARY.md`
</output>
