---
phase: 23-spatial-interaction-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/contexts/GraphStateContext.tsx
autonomous: true
must_haves:
  truths:
    - "GraphStateContext provides pinnedNodes map and pinNode/unpinNode functions"
    - "Pinned node positions load from localStorage on app startup"
    - "Pinned positions persist to localStorage on any change"
    - "Unpinned nodes are not stored and use default force layout"
  artifacts:
    - path: "apps/web/src/contexts/GraphStateContext.tsx"
      provides: "Extended GraphState with pinnedNodes management"
      contains:
        - "interface GraphState { pinnedNodes: Record<string, {x: number, y: number}> }"
        - "const pinNode = useCallback(...)"
        - "const unpinNode = useCallback(...)"
  key_links:
    - from: "localStorage"
      to: "GraphStateContext.pinnedNodes"
      via: "useState initializer and useEffect"
      pattern: "localStorage\\.getItem.*PINNED|PINNED.*localStorage\\.setItem"
---

<objective>
Extend GraphStateContext to manage pinned node positions with localStorage persistence.

Purpose: Pinned state is UI-only and must not be stored in note frontmatter. Using a separate localStorage mapping keeps note data clean and allows users to customize graph layout without affecting markdown files.

Output: GraphStateContext with pinnedNodes state, pinNode/unpinNode actions, and automatic localStorage sync.

</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-spatial-interaction-linking/23-CONTEXT.md
@.planning/phases/23-spatial-interaction-linking/23-RESEARCH.md

# Reference for existing patterns
@apps/web/src/hooks/useDraftPersistence.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GraphState interface and default state</name>
  <files>apps/web/src/contexts/GraphStateContext.tsx</files>
  <action>
    1. Add `pinnedNodes: Record<string, { x: number; y: number }> = {}` to `GraphState` interface.
    2. Initialize `pinnedNodes: {}` in `defaultState`.
    3. Add `pinNode` and `unpinNode` function signatures to `GraphStateContextType`.
  </action>
  <verify>TypeScript compiles without errors in GraphStateContext.tsx</verify>
  <done>GraphState interface includes pinnedNodes; context type declares pinNode/unpinNode</done>
</task>

<task type="auto">
  <name>Task 2: Implement pin/unpin actions and localStorage persistence</name>
  <files>apps/web/src/contexts/GraphStateContext.tsx</files>
  <action>
    1. In `GraphStateProvider`, implement `pinNode`:
       - setGraphState(prev => ({ pinnedNodes: { ...prev.pinnedNodes, [id]: { x, y } } }))
    2. Implement `unpinNode`:
       - setGraphState(prev => { const { [id]: _, ...rest } = prev.pinnedNodes; return { pinnedNodes: rest }; })
    3. Add localStorage persistence using a `useEffect`:
       - On mount: read `localStorage.getItem('notetaiker:graph:pinned-nodes')`, parse JSON, set initial state
       - On pinnedNodes change: `localStorage.setItem('notetaiker:graph:pinned-nodes', JSON.stringify(pinnedNodes))`
       - Add try/catch for QuotaExceededError and JSON parse errors, log warnings to console
    4. Include `pinNode` and `unpinNode` in the context `value` object.
    5. Follow the pattern from `useDraftPersistence.ts` for localStorage handling.
  </action>
  <verify>
    - Run TypeScript build: pnpm build (or pnpm --filter @notetaiker/web build)
    - Manual test: Pin a node via ForceGraph props (temporary), then check localStorage has the key with JSON
  </verify>
  <done>
    - pinNode/unpinNode functions work correctly
    - Pinned positions survive page reload via localStorage
    - localStorage key is `notetaiker:graph:pinned-nodes`
  </done>
</task>

</tasks>

<verification>
After building, confirm GraphStateContext exports pinNode/unpinNode and that the context provider includes them. Check that localStorage contains pinned-nodes key after pinning a node (using browser dev tools).

</verification>

<success_criteria>
- GraphStateContext has pinnedNodes state with at least 50% test coverage via later integration
- pinNode and unpinNode are callable from ForceGraph component
- Pinned positions survive browser refresh and are stored in localStorage
- No runtime errors on load if localStorage has malformed data
</success_criteria>

<output>
After completion, create `.planning/phases/23-spatial-interaction-linking/23-01-SUMMARY.md`
</output>
