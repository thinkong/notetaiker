---
phase: 09-ai-provider-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["apps/api/src/services/ai.service.ts", "apps/web/src/components/settings/ProviderSection.tsx", "packages/env/index.ts"]
autonomous: false
must_haves:
  truths:
    - "AI processing works even if Base URL is empty in settings"
    - "Settings UI shows default URLs as placeholders"
    - "User can select from a list of models or enter a custom one"
  artifacts:
    - path: "apps/api/src/services/ai.service.ts"
      provides: "Runtime default URL handling"
    - path: "apps/web/src/components/settings/ProviderSection.tsx"
      provides: "Hybrid model selection UI"
  key_links:
    - from: "apps/web/src/components/settings/ProviderSection.tsx"
      to: "apps/api/src/routes/settings.ts"
      via: "validate mutation"
---

<objective>
Implement default Base URLs for AI providers and enhance model selection with hybrid manual/list input.

Purpose: Ensures NoteTaiker remains functional with minimal configuration while providing a polished, resilient setup experience.
Output: Updated AI service with default URL logic and a refined settings UI.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ai-provider-resilience/09-CONTEXT.md
@apps/api/src/services/ai.service.ts
@apps/web/src/components/settings/ProviderSection.tsx
@apps/api/src/routes/settings.ts
</context>

<tasks>

<task type="auto">
  <name>Update AIService with default Base URLs</name>
  <files>apps/api/src/services/ai.service.ts</files>
  <action>
    Modify the `getModel` method to provide standard default URLs if `baseUrl` is missing or empty string.
    - OpenAI: `https://api.openai.com/v1`
    - Anthropic: `https://api.anthropic.com/v1`
    - Gemini: `https://generativelanguage.googleapis.com/v1beta`
    Note: `apps/api/src/routes/settings.ts` already has some of these defaults in its validation logic; ensure consistency.
  </action>
  <verify>
    Check that AI processing still functions (or fails with auth error rather than URL error) when baseUrl is cleared in the database.
  </verify>
  <done>
    `AIService` uses provider-specific defaults when `baseUrl` is undefined or empty.
  </done>
</task>

<task type="auto">
  <name>Enhance Settings UI with placeholders and model selection</name>
  <files>
    apps/web/src/components/settings/ProviderSection.tsx
    packages/env/index.ts
  </files>
  <action>
    1. Update `packages/env/index.ts` to include `model` field in `SecretsSchema` for each provider.
    2. Update `ProviderSection.tsx`:
       - Add placeholders to Base URL inputs showing the default URLs.
       - Implement a hybrid model selection input. It should show a datalist/dropdown of models fetched during "Test Connection", but allow manual text entry (fallback).
       - Ensure the `model` field is registered and saved along with API key and Base URL.
  </action>
  <verify>
    1. Open Settings page.
    2. See gray placeholders in Base URL fields.
    3. Click "Test Connection" for a provider.
    4. Verify model selection becomes available and allows both picking from list and typing custom model.
  </verify>
  <done>
    Settings UI supports default placeholders and hybrid model selection.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Default Base URLs and Model Selection</ what-built>
  <how-to-verify>
    1. Clear the Base URL for OpenAI/Anthropic in Settings and Save.
    2. Create a new note and verify that AI tags are still generated (requires valid API key).
    3. Verify that the "Test Connection" button populates the model selection list.
    4. Verify that you can still manually type a model name if desired.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- Run `npm run build` to ensure no type regressions in `packages/env` or components.
- Verify `AIService` tests (if any) pass or are updated.
</verification>

<success_criteria>
- System uses default AI URLs when fields are empty.
- Settings UI provides model discovery via "Test Connection".
- Configuration persists correctly including custom/selected models.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-provider-resilience/09-01-SUMMARY.md`
</output>
