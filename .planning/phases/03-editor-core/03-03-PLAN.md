---
phase: 03-editor-core
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified: [apps/web/src/components/editor/extensions/markdownStyle.ts, apps/web/src/components/editor/extensions/links.ts, apps/web/src/components/editor/Editor.tsx]
autonomous: true

must_haves:
  truths:
    - "Headers (#) appear larger but the '#' marker remains visible"
    - "Pressing Enter on a list item (-) creates a new list item"
    - "Cmd+Click (or Ctrl+Click) on a URL opens it in a new tab"
  artifacts:
    - path: "apps/web/src/components/editor/extensions/markdownStyle.ts"
      provides: "Visible-marker syntax highlighting styles"
    - path: "apps/web/src/components/editor/extensions/links.ts"
      provides: "Cmd+Click navigation logic"
  key_links:
    - from: "apps/web/src/components/editor/Editor.tsx"
      to: "@codemirror/lang-markdown"
      via: "Language support extension"
---

<objective>
Enhance the Markdown editing experience with specialized styling and behavior that reinforces the "Developer Focus" philosophy.

Purpose: Provide visual hierarchy and convenience without hiding the underlying Markdown structure.
Output: Integrated Markdown extensions for styling, lists, and links.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-editor-core/03-02-SUMMARY.md
@.planning/phases/03-editor-core/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Visible-Marker Styling</name>
  <files>apps/web/src/components/editor/extensions/markdownStyle.ts</files>
  <action>
    Implement a custom `HighlightStyle` using `@codemirror/language`:
    1. Define styles for tags: `heading1`, `heading2`, `strong`, `emphasis`, `link`, `url`.
    2. Set H1 to 1.5rem, H2 to 1.25rem while ensuring markers (hashes) inherit the size.
    3. Use Nord palette colors for different syntax elements (e.g., Frost for headers, Aurora for bold).
    4. Apply this via `syntaxHighlighting()` extension in `Editor.tsx`.
  </action>
  <verify>Type # Header and verify it is larger than body text but the # is visible</verify>
  <done>Markdown structure is visually reinforced without being hidden</done>
</task>

<task type="auto">
  <name>Task 2: List Continuation and Link Handling</name>
  <files>apps/web/src/components/editor/extensions/links.ts, apps/web/src/components/editor/Editor.tsx</files>
  <action>
    1. List Continuation: Add `insertNewlineContinueMarkup` from `@codemirror/commands` to the keymap in `Editor.tsx`.
    2. Link Handling: Create `links.ts` using `EditorView.domEventHandlers`.
       - Capture `mousedown` events.
       - Check if `metaKey` (Mac) or `ctrlKey` (Win/Linux) is pressed.
       - Find URL at click position and use `window.open(url, '_blank')`.
    3. Register extensions in the `Editor` component.
  </action>
  <verify>1. Create a list, press Enter, verify next bullet appears. 2. Write a URL, Cmd+Click it, verify new tab opens.</verify>
  <done>Editor supports fluid list entry and safe link navigation</done>
</task>

</tasks>

<verification>
Perform end-to-end Markdown check: Headers, Bold, Lists, and Links should all behave as specified.
</verification>

<success_criteria>
- Headers have visual hierarchy.
- List entry is frictionless.
- Links are navigable via modifier key.
</success_criteria>

<output>
After completion, create `.planning/phases/03-editor-core/03-03-SUMMARY.md`
</output>
