---
phase: 03-editor-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/web/package.json, apps/web/src/components/editor/theme.ts, apps/web/src/components/editor/Editor.tsx]
autonomous: true

must_haves:
  truths:
    - "Editor component renders without errors"
    - "Editor automatically focuses on mount"
    - "Theme matches Nord color palette (dark/light neutral)"
  artifacts:
    - path: "apps/web/src/components/editor/theme.ts"
      provides: "CodeMirror 6 Theme configuration"
    - path: "apps/web/src/components/editor/Editor.tsx"
      provides: "Basic CM6 editor wrapper"
  key_links:
    - from: "apps/web/src/components/editor/Editor.tsx"
      to: "@uiw/react-codemirror"
      via: "Component usage"
---

<objective>
Establish the technical foundation for the capture interface using CodeMirror 6 and define the Nord-inspired visual style.

Purpose: High-performance editor requires modular setup from the start.
Output: Styled Editor component with autofocus.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-editor-core/03-RESEARCH.md
@.planning/phases/03-editor-core/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Editor Dependencies</name>
  <files>apps/web/package.json</files>
  <action>
    Install the following packages in apps/web:
    - @uiw/react-codemirror
    - @codemirror/lang-markdown
    - @codemirror/language
    - @codemirror/commands
    - @codemirror/state
    - @codemirror/view
    - lodash.debounce
    - @types/lodash.debounce (dev)

    Use pnpm to install.
  </action>
  <verify>Check package.json for dependencies; run `pnpm install`</verify>
  <done>Dependencies are available in the web workspace</done>
</task>

<task type="auto">
  <name>Task 2: Implement Nord Theme and Base Editor</name>
  <files>apps/web/src/components/editor/theme.ts, apps/web/src/components/editor/Editor.tsx</files>
  <action>
    1. Create theme.ts: Define a CodeMirror 6 theme using Nord colors:
       - Background: #2e3440 (dark) / #eceff4 (light)
       - Foreground: #d8dee9 (dark) / #4c566a (light)
       - Caret: #88c0d0
       - Selection: #434c5e
       - Font: System monospace stack (ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace)
    2. Create Editor.tsx:
       - Use @uiw/react-codemirror
       - Set autoFocus: true
       - Disable line numbers and gutters for the "clean" look
       - Use basicSetup with minimal extensions (history, bracketMatching)
  </action>
  <verify>Import Editor into a dummy page and check visual appearance and autofocus</verify>
  <done>Editor renders with Nord theme and focuses on mount</done>
</task>

</tasks>

<verification>
Run `pnpm -F @notetaiker/web dev` and confirm the editor appears with the correct styling and focus.
</verification>

<success_criteria>
- Dependencies installed correctly.
- Theme matches Nord palette.
- Editor focuses immediately (sub-100ms TTI feel).
</success_criteria>

<output>
After completion, create `.planning/phases/03-editor-core/03-01-SUMMARY.md`
</output>
