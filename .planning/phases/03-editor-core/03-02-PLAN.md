---
phase: 03-editor-core
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [apps/web/src/hooks/useDebouncedSave.ts, apps/web/src/App.tsx, apps/web/src/components/layout/StatusIndicator.tsx]
autonomous: true

must_haves:
  truths:
    - "Typing in the editor triggers a debounced save to the API"
    - "Application layout is centered with a max-width container"
    - "A subtle status indicator shows 'Saved' or 'Saving...'"
  artifacts:
    - path: "apps/web/src/hooks/useDebouncedSave.ts"
      provides: "Throttled API persistence logic"
    - path: "apps/web/src/components/layout/StatusIndicator.tsx"
      provides: "UI feedback for save status"
  key_links:
    - from: "apps/web/src/hooks/useDebouncedSave.ts"
      to: "apps/web/src/lib/api.ts"
      via: "api.notes.$post call"
---

<objective>
Connect the editor to the storage engine via debounced persistence and establish the immersive "Developer Focus" layout.

Purpose: Notes must be saved automatically without interrupting the user's flow.
Output: Integrated persistence loop and centered document layout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-editor-core/03-01-SUMMARY.md
@.planning/phases/03-editor-core/03-CONTEXT.md
@apps/web/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Debounced Save Hook</name>
  <files>apps/web/src/hooks/useDebouncedSave.ts</files>
  <action>
    Create a custom hook `useDebouncedSave` that:
    1. Accepts a callback (the save function) and a delay (default 1000ms).
    2. Uses `lodash.debounce` to wrap the save function.
    3. Handles the API call to `api.notes.$post` with the editor content.
    4. Tracks and returns 'saving' state (idle, saving, saved, error).
  </action>
  <verify>Log the 'saving' state to console and verify it transitions correctly during typing</verify>
  <done>Persistence logic correctly throttles API calls</done>
</task>

<task type="auto">
  <name>Task 2: Document Layout and Status Indicator</name>
  <files>apps/web/src/App.tsx, apps/web/src/components/layout/StatusIndicator.tsx</files>
  <action>
    1. Create `StatusIndicator.tsx`: A minimal text element (e.g., bottom-right) showing current save status in a muted font. Props: `status` ('idle' | 'saving' | 'saved' | 'error').
    2. Refactor `App.tsx`:
       - Remove the 'hello world' placeholder.
       - Implement a centered layout using Tailwind: `max-w-3xl mx-auto w-full min-h-screen py-12 px-4`.
       - Initialize `useDebouncedSave`.
       - Mount the `Editor` component, connecting its `onChange` to the hook's returned handler.
       - Mount `StatusIndicator`, passing the `saving` state from the hook.
  </action>
  <verify>Check layout on different screen sizes; verify 'Saved' text appears after typing stops</verify>
  <done>User sees a clean, focused editor that saves automatically</done>
</task>

</tasks>

<verification>
Type into the editor, wait 1 second, and verify a POST request is sent to /api/notes via the browser Network tab.
</verification>

<success_criteria>
- Layout is centered and distraction-free.
- Notes persist to the backend automatically.
- Save status is visible but unobtrusive.
</success_criteria>

<output>
After completion, create `.planning/phases/03-editor-core/03-02-SUMMARY.md`
</output>
