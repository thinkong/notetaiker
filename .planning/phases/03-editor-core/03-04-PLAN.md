---
phase: 03-editor-core
plan: 04
type: execute
wave: 3
depends_on: ["02"]
files_modified: [
  "apps/web/src/index.css",
  "apps/api/src/services/storage.service.ts",
  "apps/api/src/routes/notes.ts",
  "apps/web/src/hooks/useDebouncedSave.ts",
  "apps/web/src/App.tsx"
]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Status indicator displays with the correct Nord4 color"
    - "Multiple saves during the same session update the same file"
    - "API correctly accepts and processes note IDs"
  artifacts:
    - path: "apps/web/src/hooks/useDebouncedSave.ts"
      provides: "Session-based note ID tracking"
    - path: "apps/api/src/services/storage.service.ts"
      provides: "In-place note updates by ID"
    - path: "apps/api/src/routes/notes.ts"
      provides: "API endpoint handling for ID-based updates"
  key_links:
    - from: "useDebouncedSave.ts"
      to: "api.notes.$post"
      via: "passing id in JSON body"
    - from: "apps/api/src/routes/notes.ts"
      to: "StorageService.saveNote"
      via: "passing id parameter"
---

<objective>
Close the identified gaps in Phase 03: Fix missing Nord4 theme color and implement Note ID tracking to ensure session persistence (updating instead of creating new files).

Purpose: Prevent file bloat and fix visual regressions.
Output: A stable capture interface that correctly persists a single note per session.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-editor-core/03-02-SUMMARY.md
@apps/web/src/index.css
@apps/web/src/hooks/useDebouncedSave.ts
@apps/api/src/services/storage.service.ts
@apps/api/src/routes/notes.ts
@apps/web/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Nord theme colors</name>
  <files>apps/web/src/index.css</files>
  <action>
    Add --color-nord4: #d8dee9 to the @theme block.
    Also add --color-nord5: #e5e9f0 and --color-nord6: #eceff4 to complete the Snow Storm palette for consistency.
  </action>
  <verify>Check apps/web/src/index.css for the new variables.</verify>
  <done>Nord4 color is defined and available for Tailwind classes.</done>
</task>

<task type="auto">
  <name>Task 2: API and Storage Support for Note Updates</name>
  <files>apps/api/src/services/storage.service.ts, apps/api/src/routes/notes.ts</files>
  <action>
    1. StorageService: Modify `saveNote` to handle updates. If `metadata.id` is provided, search for and overwrite the existing file. Refresh `updatedAt`.
    2. API Route: Update the POST route in `apps/api/src/routes/notes.ts` to extract `id` from the request body (`c.req.json()`) and pass it to `storageService.saveNote`.
  </action>
  <verify>Verify via manual API call that passing an existing ID in the body updates the file instead of creating a new one.</verify>
  <done>Backend correctly identifies and updates existing notes when an ID is provided.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend ID Tracking and Integration</name>
  <files>apps/web/src/hooks/useDebouncedSave.ts, apps/web/src/App.tsx</files>
  <action>
    1. useDebouncedSave: Add `noteId` state. Update the `saveFn` to include `noteId` in the request metadata. Update `noteId` with the ID returned by the API on success.
    2. App.tsx: Update the integration to ensure `saveFn` correctly handles the ID flow and that the returned status/ID from the hook are managed correctly across the session.
  </action>
  <verify>Inspect network requests: verify first save has no ID, subsequent saves pass the ID returned by the first.</verify>
  <done>The editor updates the same note file throughout the editing session.</done>
</task>

</tasks>

<verification>
1. Verify StatusIndicator is visible (Nord4 color should now resolve).
2. Create a note, type some text, wait for "Saved".
3. Check the notes directory: one file should exist.
4. Type more text, wait for "Saved".
5. Check the notes directory: still only one file should exist (the same file updated).
</verification>

<success_criteria>
- StatusIndicator renders with correct opacity and color.
- A single capture session results in a single Markdown file being updated, not multiple files created.
</success_criteria>

<output>
After completion, create `.planning/phases/03-editor-core/03-04-SUMMARY.md`
</output>
