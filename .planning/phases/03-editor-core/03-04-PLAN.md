---
phase: 03-editor-core
plan: 04
type: execute
wave: 1
depends_on: ["03-03"]
files_modified: [
  "apps/web/src/index.css",
  "apps/api/src/services/storage.service.ts",
  "apps/web/src/hooks/useDebouncedSave.ts"
]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Status indicator displays with the correct Nord4 color"
    - "Multiple saves during the same session update the same file"
  artifacts:
    - path: "apps/web/src/hooks/useDebouncedSave.ts"
      provides: "Session-based note ID tracking"
    - path: "apps/api/src/services/storage.service.ts"
      provides: "In-place note updates by ID"
  key_links:
    - from: "useDebouncedSave.ts"
      to: "api.notes.$post"
      via: "passing id in JSON body"
---

<objective>
Close the identified gaps in Phase 03: Fix missing Nord4 theme color and implement Note ID tracking to ensure session persistence (updating instead of creating new files).

Purpose: Prevent file bloat and fix visual regressions.
Output: A stable capture interface that correctly persists a single note per session.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-editor-core/03-03-SUMMARY.md
@apps/web/src/index.css
@apps/web/src/hooks/useDebouncedSave.ts
@apps/api/src/services/storage.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Nord theme colors</name>
  <files>apps/web/src/index.css</files>
  <action>
    Add --color-nord4: #d8dee9 to the @theme block.
    Also add --color-nord5: #e5e9f0 and --color-nord6: #eceff4 to complete the Snow Storm palette for consistency.
  </action>
  <verify>Check apps/web/src/index.css for the new variables.</verify>
  <done>Nord4 color is defined and available for Tailwind classes.</done>
</task>

<task type="auto">
  <name>Task 2: Support updating notes in StorageService</name>
  <files>apps/api/src/services/storage.service.ts</files>
  <action>
    Modify StorageService.saveNote to handle updates:
    1. If metadata.id is provided, search for an existing file with that ID (reuse logic from getNote if possible).
    2. If found, overwrite that specific file using writeFileAtomic.
    3. If not found or ID not provided, continue with existing logic of generating a new unique filename.

    Ensure the updatedAt timestamp is refreshed on every save.
  </action>
  <verify>Run existing storage tests or verify via manual API call that passing an existing ID overwrites the file.</verify>
  <done>StorageService can update existing notes when an ID is provided.</done>
</task>

<task type="auto">
  <name>Task 3: Track Note ID in frontend</name>
  <files>apps/web/src/hooks/useDebouncedSave.ts</files>
  <action>
    1. Add a noteId state to the useDebouncedSave hook (initialized to null).
    2. Update the save function to include the current noteId in the API request metadata if it exists.
    3. On successful save, update the noteId state with the ID returned by the API response.
    4. Ensure handleContentChange still triggers the debounced save correctly.
  </action>
  <verify>Inspect network requests in the browser: first save should have no ID, subsequent saves should pass the same ID.</verify>
  <done>The editor updates the same note file throughout the editing session.</done>
</task>

</tasks>

<verification>
1. Verify StatusIndicator is visible (Nord4 color should now resolve).
2. Create a note, type some text, wait for "Saved".
3. Check the notes directory: one file should exist.
4. Type more text, wait for "Saved".
5. Check the notes directory: still only one file should exist (or the same file updated).
</verification>

<success_criteria>
- StatusIndicator renders with correct opacity and color.
- A single capture session results in a single Markdown file being updated, not multiple files created.
</success_criteria>

<output>
After completion, create `.planning/phases/03-editor-core/03-04-SUMMARY.md`
</output>
