---
phase: 20-related-notes
plan: 03
type: execute
wave: 3
depends_on: [20-02]
files_modified: [apps/web/src/components/sidebar/RelatedNotesPanel.tsx]
autonomous: true
must_haves:
  truths:
    - "Similarity levels (e.g., 'High', 'Medium') are displayed for related notes"
    - "The UI handles the case where no embeddings exist yet for a note"
    - "Fetching is debounced to avoid excessive API calls during rapid navigation"
  artifacts:
    - path: "apps/web/src/components/sidebar/RelatedNotesPanel.tsx"
      provides: "Refined UX with similarity labels and debounce"
  key_links: []
---

<objective>
Refine the Related Notes UX with visual similarity indicators, debouncing, and robust empty states.

Purpose: Improve the user experience and perceived performance of the semantic discovery feature.
Output: A polished Related Notes panel that provides intuitive feedback on the strength of connections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-related-notes/20-02-SUMMARY.md
@apps/web/src/components/sidebar/RelatedNotesPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Implement Similarity Labels</name>
  <files>apps/web/src/components/sidebar/RelatedNotesPanel.tsx</files>
  <action>
    1. Map the raw `distance` (cosine distance) to human-readable labels.
       - Note: sqlite-vec cosine distance ranges from 0 (identical) to 2 (opposite).
       - Suggestion: distance < 0.2 => "Highly Related", distance < 0.4 => "Related", distance < 0.6 => "Somewhat Related".
    2. Display these labels or a visual indicator (e.g., a small strength bar) next to each related note.
  </action>
  <verify>Observe the related notes list and confirm labels match the expected similarity levels.</verify>
  <done>Users see intuitive labels instead of raw distance values.</done>
</task>

<task type="auto">
  <name>Add Fetch Debouncing</name>
  <files>apps/web/src/components/sidebar/RelatedNotesPanel.tsx</files>
  <action>
    1. Implement a small delay (e.g., 300-500ms) before fetching related notes when the active note changes.
    2. This prevents "flicker" and unnecessary API calls when a user is quickly clicking through the timeline or note list.
  </action>
  <verify>Quickly switch between several notes and observe that the API request is only made for the "settled" note.</verify>
  <done>Fetching is efficient and avoids redundant network traffic.</done>
</task>

<task type="auto">
  <name>Refine Empty and Error States</name>
  <files>apps/web/src/components/sidebar/RelatedNotesPanel.tsx</files>
  <action>
    1. Specifically handle the "empty" response from the API (which occurs if a note hasn't been indexed yet).
    2. Show a friendly message like "Analyzing connections..." or "No similar notes discovered yet."
    3. Ensure smooth transitions between loading and data states.
  </action>
  <verify>Test with a brand new note (not yet indexed) and verify the "Analyzing..." or similar message appears.</verify>
  <done>The UI gracefully handles notes that are not yet processed by the AI pipeline.</done>
</task>

</tasks>

<verification>
Perform a final end-to-end walk-through of the feature: open note, view related, click related, verify labels and debouncing.
</verification>

<success_criteria>

- Related notes show labels like "Highly Related".
- Switching notes quickly does not trigger multiple immediate API calls.
- Empty states are helpful and informative.
  </success_criteria>

<output>
After completion, create `.planning/phases/20-related-notes/20-03-SUMMARY.md`
</output>
