---
phase: 20-related-notes
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  [
    apps/web/src/components/sidebar/RelatedNotesPanel.tsx,
    apps/web/src/components/sidebar/Sidebar.tsx,
  ]
autonomous: true
must_haves:
  truths:
    - "A 'Related Notes' section is visible in the sidebar"
    - "The section lists notes returned by the related notes API"
    - "Clicking a note in the related list switches the editor context to that note"
  artifacts:
    - path: "apps/web/src/components/sidebar/RelatedNotesPanel.tsx"
      provides: "List display of semantically related notes"
    - path: "apps/web/src/components/sidebar/Sidebar.tsx"
      provides: "Integration point for the RelatedNotesPanel"
  key_links:
    - from: "apps/web/src/components/sidebar/RelatedNotesPanel.tsx"
      to: "/api/notes/:id/related"
      via: "TanStack Query"
---

<objective>
Create the frontend UI component to display related notes in the sidebar.

Purpose: Surface semantic connections directly to the user in their current context.
Output: A new sidebar panel that shows related notes for the currently active note.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-related-notes/20-01-SUMMARY.md
@apps/web/src/components/sidebar/Sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Create RelatedNotesPanel Component</name>
  <files>apps/web/src/components/sidebar/RelatedNotesPanel.tsx</files>
  <action>
    1. Implement a `RelatedNotesPanel` component that takes a `noteId`.
    2. Use `useQuery` to fetch from `/api/notes/${noteId}/related`.
    3. Render a list of related notes. Use a similar style to `SidebarNoteCard` but more compact if necessary.
    4. Handle loading and empty states (e.g., "No related notes found" or "Note not yet indexed").
    5. Add navigation logic: clicking a note should call the app's navigation function (likely via React Router or a central state update).
  </action>
  <verify>Run the app and check if the component renders related notes when a note is selected.</verify>
  <done>RelatedNotesPanel displays similar notes and supports navigation.</done>
</task>

<task type="auto">
  <name>Integrate into Sidebar</name>
  <files>apps/web/src/components/sidebar/Sidebar.tsx</files>
  <action>
    1. Import `RelatedNotesPanel` into `Sidebar.tsx`.
    2. Add the panel to the sidebar layout.
    3. Since the sidebar already has a "Timeline" view, consider if this should be a new tab or an additional section. Based on the "Milestone" goal, a dedicated section or tab for "Related" makes sense.
    4. Pass the current active note ID to the component.
  </action>
  <verify>Check that the 'Related Notes' section appears in the sidebar.</verify>
  <done>Sidebar displays the RelatedNotesPanel for the current note.</done>
</task>

</tasks>

<verification>
Ensure that switching between notes updates the related notes list (it should be reactive to the active note ID).
</verification>

<success_criteria>

- Opening a note triggers a fetch for related notes.
- Related notes are displayed in the sidebar.
- Clicking a related note opens it in the editor.
  </success_criteria>

<output>
After completion, create `.planning/phases/20-related-notes/20-02-SUMMARY.md`
</output>
