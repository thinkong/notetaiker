---
phase: 15-history-integration-fresh-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/web/src/hooks/useDebouncedSave.ts, apps/web/src/App.tsx]
autonomous: true

must_haves:
  truths:
    - "Changing the note ID in the persistence hook triggers a re-render in the App component"
    - "Clicking a (placeholder) New Note action clears the editor and resets the persistence ID"
  artifacts:
    - path: "apps/web/src/hooks/useDebouncedSave.ts"
      provides: "Reactive noteId state"
    - path: "apps/web/src/App.tsx"
      provides: "handleNewNote logic"
  key_links:
    - from: "apps/web/src/App.tsx"
      to: "apps/web/src/hooks/useDebouncedSave.ts"
      via: "currentNoteId state"
---

<objective>
Expose the current note ID as reactive state and implement the 'New Note' reset logic to support transitioning between draft and editing modes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/hooks/useDebouncedSave.ts
@apps/web/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Expose noteId state in useDebouncedSave</name>
  <files>apps/web/src/hooks/useDebouncedSave.ts</files>
  <action>
    Convert noteIdRef to a useState hook or keep a state synchronized with the ref.
    Ensure setNoteId and clearNoteId update this state.
    Return noteId in the hook's return object.
    This allows App.tsx and other components to know WHICH note is currently active.
  </action>
  <verify>
    Check that useDebouncedSave now returns noteId.
  </verify>
  <done>
    noteId is returned from the hook and updates correctly when setNoteId/clearNoteId are called.
  </done>
</task>

<task type="auto">
  <name>Implement handleNewNote in App.tsx</name>
  <files>apps/web/src/App.tsx</files>
  <action>
    1. Extract noteId from useDebouncedSave.
    2. Implement handleNewNote function:
       - Cancel any pending debounced saves.
       - Call clearNoteId().
       - setContent("") or reset to a minimalist welcome state.
       - Call clearDraft() from useDraftPersistence.
       - Call markClean() from useUnsavedChanges.
       - Focus the editor using editorRef.current?.focus().
    3. Add a temporary "New" button in the header (will be polished in plan 03) to test the logic.
  </action>
  <verify>
    Clicking the New button clears the editor and ensures the internal noteId is null.
  </verify>
  <done>
    A functioning handleNewNote action exists and resets all relevant state.
  </done>
</task>

</tasks>

<verification>
Verify that clearing a note while editing an existing one actually resets the state to "Draft" mode (noteId: null).
</verification>

<success_criteria>
The application can transition from an 'Editing' state (with a noteId) back to a 'Draft' state (noteId: null) via an explicit action.
</success_criteria>

<output>
After completion, create `.planning/phases/15-history-integration-fresh-capture/15-01-SUMMARY.md`
</output>
