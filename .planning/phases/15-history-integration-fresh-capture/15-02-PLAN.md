---
phase: 15-history-integration-fresh-capture
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified: [apps/web/src/components/sidebar/SidebarNoteCard.tsx, apps/web/src/components/sidebar/SidebarTimeline.tsx, apps/web/src/App.tsx]
autonomous: true

must_haves:
  truths:
    - "The currently active note is visually highlighted in the sidebar"
    - "Switching between notes shows a clear loading indication if the fetch is not instantaneous"
  artifacts:
    - path: "apps/web/src/components/sidebar/SidebarNoteCard.tsx"
      provides: "Active state highlighting"
    - path: "apps/web/src/App.tsx"
      provides: "Loading state management for note fetching"
  key_links:
    - from: "apps/web/src/components/sidebar/SidebarNoteCard.tsx"
      to: "App.tsx"
      via: "currentNoteId prop"
---

<objective>
Integrate the active note state with the sidebar for visual feedback and implement a skeleton loading state for note transitions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/web/src/components/sidebar/SidebarNoteCard.tsx
@apps/web/src/components/sidebar/SidebarTimeline.tsx
@apps/web/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Highlight active note in sidebar</name>
  <files>
    apps/web/src/components/sidebar/SidebarNoteCard.tsx,
    apps/web/src/components/sidebar/SidebarTimeline.tsx,
    apps/web/src/App.tsx
  </files>
  <action>
    1. Update SidebarTimeline and SidebarNoteCard to accept an `activeNoteId` prop.
    2. In App.tsx, pass the `noteId` from useDebouncedSave to SidebarTimeline.
    3. In SidebarNoteCard.tsx, apply a visual highlight (e.g., ring-2 ring-nord-frost3) if `metadata.id === activeNoteId`.
    4. Ensure the highlight is subtle but clear.
  </action>
  <verify>
    Select a note from the history and verify it remains highlighted in the sidebar while being edited.
  </verify>
  <done>
    The sidebar correctly reflects the note currently loaded in the editor.
  </done>
</task>

<task type="auto">
  <name>Implement skeleton loading for note fetch</name>
  <files>apps/web/src/App.tsx</files>
  <action>
    1. Add an `isLoadingNote` state to MainCapture.
    2. In `handleEditNote`, set `isLoadingNote(true)` before the API call and `false` after.
    3. In the render logic, if `isLoadingNote` is true, replace the Editor with a skeleton component (matching the Editor's height and layout roughly).
    4. Ensure the transition is snappy.
  </action>
  <verify>
    Click a note to edit and observe a brief skeleton state before the content populates.
  </verify>
  <done>
    Transitions between notes have a clear loading affordance that prevents layout shift.
  </done>
</task>

</tasks>

<verification>
Verify that clicking 'New Note' (from Plan 01) removes the highlight from any previously selected note in the sidebar.
</verification>

<success_criteria>
The UI provides consistent feedback about which note is active and its loading status.
</success_criteria>

<output>
After completion, create `.planning/phases/15-history-integration-fresh-capture/15-02-SUMMARY.md`
</output>
