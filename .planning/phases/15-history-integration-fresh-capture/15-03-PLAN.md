---
phase: 15-history-integration-fresh-capture
plan: 03
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified: [apps/web/src/App.tsx, apps/web/src/components/layout/StatusIndicator.tsx]
autonomous: true

must_haves:
  truths:
    - "A badge clearly indicates if the user is in 'Draft' or 'Editing' mode"
    - "The primary action button text changes based on the mode (e.g., 'Capture' vs 'Save')"
    - "Pressing Cmd+N/Ctrl+N triggers a new note creation"
  artifacts:
    - path: "apps/web/src/App.tsx"
      provides: "Mode-aware UI labels and hotkeys"
  key_links:
    - from: "UI"
      to: "handleNewNote"
      via: "useHotkeys(mod+n)"
---

<objective>
Refine the UI with mode-specific visuals, dynamic labels, and keyboard shortcuts to provide a polished history integration experience.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/web/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Add Mode Badge and Dynamic Labels</name>
  <files>apps/web/src/App.tsx</files>
  <action>
    1. Define `isEditing = !!noteId`.
    2. Add a status badge next to the "notetAIker" title or in the toolbar that says "Draft" (Nord polar3) or "Editing" (Nord frost3).
    3. Update the primary Save button text:
       - If `!isEditing`: "Capture"
       - If `isEditing`: "Save"
    4. Ensure the Save button's icon or styling matches the established design but reflects the intent.
  </action>
  <verify>
    Verify labels change correctly when switching between a new note and an existing one.
  </verify>
  <done>
    The UI clearly communicates the current editor mode via labels and badges.
  </done>
</task>

<task type="auto">
  <name>Register Cmd+N Hotkey</name>
  <files>apps/web/src/App.tsx</files>
  <action>
    1. Use `useHotkeys` to register `mod+n`.
    2. Prevent default and call `handleNewNote`.
    3. Ensure it works even when the editor is focused (pass `{ enableOnFormTags: true }`).
  </action>
  <verify>
    Press Cmd+N (or Ctrl+N) and verify the editor resets to a fresh capture state.
  </verify>
  <done>
    Users can trigger a new note creation via a standard keyboard shortcut.
  </done>
</task>

<task type="auto">
  <name>Refine StatusIndicator for Modes</name>
  <files>apps/web/src/App.tsx, apps/web/src/components/layout/StatusIndicator.tsx</files>
  <action>
    Ensure the StatusIndicator (idle/saving/saved) doesn't clash with the new Mode Badge.
    Optionally integrate them if it makes more sense (e.g., "Draft (unsaved)" vs "Saved").
    For this phase, keep them distinct but visually aligned.
  </action>
  <verify>
    Check for visual overlap or confusion between the Mode Badge and the Save Status.
  </verify>
  <done>
    Mode indicators and save status work harmoniously together.
  </done>
</task>

</tasks>

<verification>
Perform a full cycle: Start app -> Type (Draft) -> Save (Becomes Editing/Saved) -> Edit history note -> Highlight changes -> Cmd+N -> Editor clears (Draft).
</verification>

<success_criteria>
The 'New Note' flow is discoverable, fast, and clearly distinguished from the editing flow.
</success_criteria>

<output>
After completion, create `.planning/phases/15-history-integration-fresh-capture/15-03-SUMMARY.md`
</output>
