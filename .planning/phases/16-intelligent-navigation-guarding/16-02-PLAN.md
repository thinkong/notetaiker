---
phase: 16-intelligent-navigation-guarding
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - apps/web/src/hooks/useNavigationGuard.ts
  - apps/web/src/App.tsx
  - apps/web/src/hooks/useUnsavedChanges.ts
  - apps/api/src/services/storage.service.ts
autonomous: true

must_haves:
  truths:
    - "Navigation away from modified notes triggers a confirmation dialog"
    - "Navigating away from unmodified notes does NOT trigger a prompt"
    - "Browser refresh triggers native confirmation"
  artifacts:
    - path: "apps/web/src/hooks/useNavigationGuard.ts"
      provides: "Navigation blocking logic"
    - path: "apps/web/src/App.tsx"
      provides: "Smart dirty check implementation"
  key_links:
    - from: "MainCapture (isDirty)"
      to: "useNavigationGuard"
      via: "React hook props"
    - from: "useNavigationGuard"
      to: "react-router-dom"
      via: "useBlocker"
---

<objective>
Implement the core navigation guarding logic and the "Smart Dirty Check" system to protect users from data loss without unnecessary prompts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@apps/web/src/App.tsx
@apps/web/src/hooks/useNavigationGuard.ts
@apps/api/src/services/storage.service.ts
</context>

<tasks>

<task type="auto">
  <name>Create useNavigationGuard hook</name>
  <files>apps/web/src/hooks/useNavigationGuard.ts</files>
  <action>
    - Implement `useNavigationGuard` hook.
    - Use `useBlocker` from React Router v7 to intercept in-app navigation when the state is dirty.
    - Implement a `beforeunload` listener to protect against browser-level events (refresh/tab close).
    - Provide a `requestAction` function that allows guarding internal state changes (like clicking "New Note") that don't trigger a route change.
  </action>
  <verify>
    - Ensure the hook compiles and correctly uses `useBlocker`.
  </verify>
  <done>
    - Hook provides `isBlocked`, `requestAction`, `proceed`, `reset`, and `saveAndProceed` capabilities.
  </done>
</task>

<task type="auto">
  <name>Implement Smart Dirty Check and Integrate Guard</name>
  <files>apps/web/src/App.tsx</files>
  <action>
    - Track `originalContent` alongside `content` in `MainCapture`.
    - Update `originalContent` when a note is successfully loaded or saved.
    - Implement `isDirty` using `useMemo` to compare trimmed `content` with trimmed `originalContent`.
    - Pass `isDirty` and an `onSave` handler to `useNavigationGuard`.
    - **Wiring Detail**: The `isDirty` boolean calculated in `MainCapture` is passed as a prop to `useNavigationGuard`. This hook internally uses `useBlocker` from `react-router-dom`, which automatically intercepts any navigation attempts within the `RouterProvider` context defined in `App.tsx`.
    - Replace legacy `useUnsavedChanges` usage with the new guard.
    - Update `ConfirmDialog` to use the blocker's `proceed`, `reset`, and `saveAndProceed` methods.
  </action>
  <verify>
    - Edit a note and try to navigate to "Graph" or "Settings". Confirmation dialog should appear.
    - Edit a note, then revert the changes manually. Navigation should now be allowed without a prompt.
  </verify>
  <done>
    - User is protected from accidental data loss by a smart, non-intrusive guard.
  </done>
</task>

<task type="auto">
  <name>Cleanup and Fix API Types</name>
  <files>
    - apps/web/src/hooks/useUnsavedChanges.ts
    - apps/api/src/services/storage.service.ts
  </files>
  <action>
    - Delete the deprecated `apps/web/src/hooks/useUnsavedChanges.ts` file.
    - In `apps/api/src/services/storage.service.ts`, ensure that `fullMetadata` explicitly uses the `NoteMetadata` type to prevent potential build errors during the merge.
  </action>
  <verify>
    - Run `pnpm build` to ensure no type regressions.
  </verify>
  <done>
    - Codebase is clean of legacy guard logic and builds successfully.
  </done>
</task>

</tasks>

<verification>
- [ ] Navigating away from a modified note triggers a confirmation dialog.
- [ ] Navigating away from an unmodified note (even if edited then reverted) does NOT trigger a prompt.
- [ ] Browser refresh/close triggers the native confirmation dialog.
- [ ] Clicking "Save" in the confirmation dialog persists the note before navigating.
- [ ] Clicking "Discard" proceeds without saving.
- [ ] Clicking "Cancel" stays on the current page.
- [ ] Project builds successfully with `pnpm build`.
</verification>

<success_criteria>
- Navigation guarding is robust, "smart", and fully integrated with the Data Router.
</success_criteria>

<output>
After completion, create `.planning/phases/16-intelligent-navigation-guarding/16-02-SUMMARY.md`
</output>
