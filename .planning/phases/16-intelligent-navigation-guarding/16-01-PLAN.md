---
phase: 16-intelligent-navigation-guarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/App.tsx
  - apps/web/src/main.tsx
autonomous: true

must_haves:
  truths:
    - "Application uses createBrowserRouter (Data Router)"
    - "Layout is preserved across route changes"
  artifacts:
    - path: "apps/web/src/App.tsx"
      provides: "Data Router configuration"
  key_links:
    - from: "apps/web/src/main.tsx"
      to: "apps/web/src/App.tsx"
      via: "RouterProvider"
---

<objective>
Migrate the application from `BrowserRouter` to `createBrowserRouter` (Data Router) to enable React Router v7's `useBlocker` hook, which is required for the navigation guard.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@apps/web/src/App.tsx
@apps/web/src/main.tsx
</context>

<tasks>

<task type="auto">
  <name>Migrate to createBrowserRouter in App.tsx</name>
  <files>apps/web/src/App.tsx</files>
  <action>
    - Replace `BrowserRouter`, `Routes`, and `Route` components with `createBrowserRouter` configuration.
    - Define the routes configuration object with an `Outlet` in a layout component to ensure background and UI persistence.
    - Ensure `MainCapture` remains the root route at `/`.
    - Update imports to include `createBrowserRouter`, `RouterProvider`, and `Outlet` from `react-router-dom`.
  </action>
  <verify>
    - Run `pnpm --filter @notetaiker/web dev` and check for console errors related to routing.
  </verify>
  <done>
    - Application loads and the router is initialized using `createBrowserRouter`.
  </done>
</task>

<task type="auto">
  <name>Verify Router functionality</name>
  <files>apps/web/src/main.tsx</files>
  <action>
    - Ensure `main.tsx` passes the router to `RouterProvider`.
    - Test navigation to `/graph` and `/settings` via the UI.
    - Verify that the Nord background layout is preserved across all routes.
  </action>
  <verify>
    - Navigate between all application routes and observe layout persistence.
    - Check browser back/forward button behavior.
  </verify>
  <done>
    - All existing routes work correctly with the new Data Router.
  </done>
</task>

</tasks>

<verification>
- App loads without errors.
- Navigation to `/graph` and `/settings` works.
- Back button in browser works as expected.
- No "useBlocker must be used within a data router" errors in console.
</verification>

<success_criteria>

- Application successfully migrated to Data Router API.
  </success_criteria>

<output>
After completion, create `.planning/phases/16-intelligent-navigation-guarding/16-01-SUMMARY.md`
</output>
