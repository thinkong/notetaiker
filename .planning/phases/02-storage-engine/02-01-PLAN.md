---
phase: 02-storage-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["apps/api/package.json", "packages/env/index.ts", "apps/api/src/index.ts"]
autonomous: true
must_haves:
  truths:
    - "API can access a configured NOTES_DIR from environment"
    - "API creates the NOTES_DIR if it does not exist on startup"
  artifacts:
    - path: "packages/env/index.ts"
      provides: "NOTES_DIR environment variable validation"
    - path: "apps/api/src/index.ts"
      provides: "Startup directory check"
---

<objective>
Configure the environment and dependencies for the storage engine.
This establishes the foundation for where notes will be stored and ensures the necessary libraries are available.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-storage-engine/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install storage dependencies</name>
  <files>apps/api/package.json</files>
  <action>
    Install core storage libraries in apps/api:
    - write-file-atomic (^6.0.0)
    - gray-matter (^4.0.3)
    - uuid (^11.0.0)
    - date-fns (^4.0.0)
    And their types:
    - @types/write-file-atomic
    - @types/uuid
    (gray-matter includes types or they are simple enough to handle if missing)

    Also install test runner:
    - vitest
    - @vitest/ui

    Add a "test": "vitest" script to apps/api/package.json.

    Use pnpm to add these to the api package.
  </action>
  <verify>Check apps/api/package.json for dependencies</verify>
  <done>Dependencies are installed and listed in package.json</done>
</task>

<task type="auto">
  <name>Task 2: Configure NOTES_DIR in environment</name>
  <files>packages/env/index.ts</files>
  <action>
    Update the Zod schema in packages/env/index.ts to include NOTES_DIR.
    - Path: string
    - Default: "./data/notes"
    Ensure it is exported as part of the env object.

    Implementation Note: When resolving the path, ensure it is relative to the workspace root if it is a relative path.
  </action>
  <verify>Run a small script to log env.NOTES_DIR</verify>
  <done>env.NOTES_DIR is defined and defaults to "./data/notes"</done>
</task>

<task type="auto">
  <name>Task 3: Initialize notes directory on startup</name>
  <files>apps/api/src/index.ts</files>
  <action>
    Modify apps/api/src/index.ts to ensure the directory specified by env.NOTES_DIR exists.
    Use fs.mkdirSync(path, { recursive: true }) before starting the server.
    Ensure the path is resolved relative to the workspace root using path.resolve(process.cwd(), env.NOTES_DIR).
    Log the location of the notes directory on startup.
  </action>
  <verify>Run the API and check if ./data/notes is created</verify>
  <done>NOTES_DIR is created automatically if missing</done>
</task>

</tasks>

<verification>
Check if ./data/notes exists after running the API dev server.
</verification>

<success_criteria>
The API starts, dependencies are present, and the storage directory is initialized.
</success_criteria>

<output>
After completion, create `.planning/phases/02-storage-engine/02-01-SUMMARY.md`
</output>
