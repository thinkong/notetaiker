---
phase: 02-storage-engine
plan: 02
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  ["apps/api/src/services/storage.service.ts", "apps/api/src/lib/markdown.ts"]
autonomous: true
must_haves:
  truths:
    - "System can write a note file with atomic guarantees"
    - "Note files contain valid YAML frontmatter with UUID and timestamps"
    - "Reading a note file returns both metadata and content"
  artifacts:
    - path: "apps/api/src/services/storage.service.ts"
      provides: "FileSystem storage abstraction"
    - path: "apps/api/src/lib/markdown.ts"
      provides: "Markdown/Frontmatter processing"
---

<objective>
Implement the core Storage Service using TDD.
This service will handle the atomic writing of Markdown files and the parsing of YAML frontmatter.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-storage-engine/02-RESEARCH.md
@.planning/phases/02-storage-engine/02-01-PLAN.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Note persistence with atomic writes</name>
  <files>apps/api/src/services/storage.service.ts, apps/api/src/services/storage.service.test.ts</files>
  <behavior>
    saveNote(content: string, metadata: any):
    - Generates YYYYMMDD-HHMMSS.md filename
    - Inserts UUID and timestamps into YAML frontmatter
    - Uses write-file-atomic for persistence
    - Handles timestamp collisions by appending suffix (_1, _2)
  </behavior>
  <test_commands>
    pnpm test apps/api/src/services/storage.service.test.ts
  </test_commands>
  <expected_outcomes>
    - Multiple notes saved with unique filenames
    - Collision logic verified by mocking time or rapid saves
    - File contents contain correct frontmatter and content
  </expected_outcomes>
  <implementation>
    Use 'write-file-atomic', 'gray-matter', and 'uuid'.
    Implement filename generation using date-fns.
  </implementation>
  <verify>Run the service tests and verify files created in temporary directory</verify>
  <done>saveNote works as specified with atomic guarantees and collision handling</done>
</task>

<task type="tdd">
  <name>Task 2: Note retrieval and parsing</name>
  <files>apps/api/src/services/storage.service.ts, apps/api/src/lib/markdown.ts, apps/api/src/services/storage.service.test.ts</files>
  <behavior>
    1. getNote(id: string): Finds note by UUID or filename, returns parsed { content, metadata }
    2. listNotes(): Returns all notes with metadata (sorted by date)
    - Uses apps/api/src/lib/markdown.ts for frontmatter validation via Zod
  </behavior>
  <test_commands>
    pnpm test apps/api/src/services/storage.service.test.ts
  </test_commands>
  <expected_outcomes>
    - getNote returns correct data for valid note
    - getNote returns null/throws for missing note
    - listNotes returns all notes in the directory with correct metadata
  </expected_outcomes>
  <implementation>
    Implement directory scanning in storage service.
    Create markdown.ts utility for consistent frontmatter parsing/validation using Zod and gray-matter.
  </implementation>
  <verify>Run the service tests</verify>
  <done>getNote and listNotes are fully functional and validated</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/02-storage-engine/02-02-SUMMARY.md`
</output>
