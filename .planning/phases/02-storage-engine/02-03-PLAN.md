---
phase: 02-storage-engine
plan: 03
type: tdd
wave: 3
depends_on: ["02-02"]
files_modified: ["apps/api/src/routes/notes.ts", "apps/api/src/index.ts"]
autonomous: true
must_haves:
  truths:
    - "API returns a list of notes at GET /notes"
    - "API creates a new note at POST /notes"
    - "API returns a specific note at GET /notes/:id"
  artifacts:
    - path: "apps/api/src/routes/notes.ts"
      provides: "Hono routes for note operations"
  key_links:
    - from: "apps/api/src/routes/notes.ts"
      to: "apps/api/src/services/storage.service.ts"
      via: "Dependency injection or direct call"
---

<objective>
Expose the Storage Service functionality via a RESTful API using Hono.
This enables the frontend to interact with the storage engine in the next phase.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-storage-engine/02-02-PLAN.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Implement Note routes</name>
  <files>apps/api/src/routes/notes.ts, apps/api/src/routes/notes.test.ts</files>
  <behavior>
    1. GET /: Calls storageService.listNotes() and returns array.
    2. POST /: Validates body { content: string, metadata?: any }, calls storageService.saveNote(), returns 201.
    3. GET /:id: Calls storageService.getNote(id), returns 200 or 404 if missing.
  </behavior>
  <test_commands>
    pnpm test apps/api/src/routes/notes.test.ts
  </test_commands>
  <expected_outcomes>
    - GET / returns 200 with list of notes
    - POST / returns 201 with saved note
    - GET /:id returns 200 with note or 404 if not found
    - POST with invalid body returns 400
  </expected_outcomes>
  <implementation>
    Define the routes in 'apps/api/src/routes/notes.ts' using Hono.
    Import and use the StorageService (from apps/api/src/services/storage.service.ts) to handle data operations.
    Use Hono's Zod validator for POST body validation.
  </implementation>
  <verify>Run the route tests</verify>
  <done>Note routes are implemented and correctly integrated with StorageService</done>
</task>

<task type="auto">
  <name>Task 2: Mount routes and export AppType</name>
  <files>apps/api/src/index.ts</files>
  <action>
    Mount the notes routes in the main Hono app at '/notes'.
    Ensure the main app exports its type as 'AppType' to enable RPC support for the client.
  </action>
  <verify>Check apps/api/src/index.ts for app.route('/notes', ...) call</verify>
  <done>Routes are mounted and AppType is exported</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/02-storage-engine/02-03-SUMMARY.md`
</output>
